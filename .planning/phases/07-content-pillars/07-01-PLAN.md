---
phase: 07-content-pillars
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/db/schema.ts
  - src/routes/dashboard/onboarding/deep-brief/+page.server.ts
  - src/routes/dashboard/onboarding/deep-brief/+page.svelte
  - messages/en.json
  - messages/es.json
autonomous: true
requirements: [PILR-01, PILR-02]

must_haves:
  truths:
    - "Personal Brand user sees Content Pillars editor in Deep Brief Section 1 instead of Product Details"
    - "User can add 1-5 content pillars, each with name and short description"
    - "Product and Service types still see the original Product Details section unchanged"
    - "Pillars are stored in contentPillars table and loaded on return visits"
    - "Pre-suggested pillar ideas reduce blank-page syndrome"
  artifacts:
    - path: "src/lib/server/db/schema.ts"
      provides: "contentPillars table definition + contentPillarsRelations"
      contains: "contentPillars"
    - path: "src/routes/dashboard/onboarding/deep-brief/+page.svelte"
      provides: "Conditional Section 1 with pillar card editor for personal_brand"
      min_lines: 100
    - path: "src/routes/dashboard/onboarding/deep-brief/+page.server.ts"
      provides: "Load/save pillars, pass productType to client"
      exports: ["load", "actions"]
    - path: "messages/en.json"
      provides: "Pillar i18n keys (en)"
      contains: "onb_brief_pillars"
    - path: "messages/es.json"
      provides: "Pillar i18n keys (es)"
      contains: "onb_brief_pillars"
  key_links:
    - from: "src/routes/dashboard/onboarding/deep-brief/+page.server.ts"
      to: "src/lib/server/db/schema.ts"
      via: "drizzle query for contentPillars"
      pattern: "contentPillars"
    - from: "src/routes/dashboard/onboarding/deep-brief/+page.svelte"
      to: "+page.server.ts"
      via: "data.productType conditionally renders pillar editor vs product details"
      pattern: "productType.*personal_brand"
---

<objective>
Add the contentPillars database table and build the conditional Deep Brief Section 1 UI that shows a card-based content pillar editor for Personal Brand users while keeping Product Details unchanged for Product/Service types.

Purpose: Personal Brand users need a way to define what topics they post about (content pillars) rather than describing product features. This is the foundational data model and UI for pillar-aware content generation.

Output: contentPillars schema table, conditional Deep Brief Section 1, i18n keys (en/es), pillar CRUD via form actions.
</objective>

<execution_context>
@/home/diego/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/server/db/schema.ts
@src/routes/dashboard/onboarding/deep-brief/+page.svelte
@src/routes/dashboard/onboarding/deep-brief/+page.server.ts
@src/routes/dashboard/+layout.server.ts
@messages/en.json
@messages/es.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + server-side pillar data loading and saving</name>
  <files>
    src/lib/server/db/schema.ts
    src/routes/dashboard/onboarding/deep-brief/+page.server.ts
  </files>
  <action>
    1. In `src/lib/server/db/schema.ts`:
       - Add `contentPillars` table with columns: `id` (uuid PK defaultRandom), `productId` (uuid FK to products.id onDelete cascade, NOT unique — a product has many pillars), `name` (text notNull), `description` (text), `sortOrder` (integer notNull default 0), `createdAt` (timestamp withTimezone defaultNow).
       - Add `contentPillarsRelations` using `relations(contentPillars, ({ one }) => ({ product: one(products, { fields: [contentPillars.productId], references: [products.id] }) }))`.
       - In `productsRelations`, add `contentPillars: many(contentPillars)` to the existing relation.

    2. In `src/routes/dashboard/onboarding/deep-brief/+page.server.ts`:
       - Import `contentPillars` from schema.
       - In `load`: Also fetch `product.productType` (it's already returned but ensure it's in the return). Query `contentPillars` for this product, ordered by `sortOrder asc`. Return `{ product, brief, productType: product.productType, pillars: existingPillars }`.
       - In the `default` action: Read `productType` from the loaded product. If `productType === 'personal_brand'`, parse a `pillars` field from formData (it will be JSON-stringified array of `{ name: string, description: string }`). Delete all existing contentPillars for this productId, then insert the new ones with sortOrder matching array index. If NOT personal_brand, ignore pillars and save the product details fields as before.
       - Keep the existing brief upsert logic for ALL types (sections 2-4 still save normally).
       - For personal_brand type, the Section 1 product detail fields (problemSolved, keyFeatures, differentiator, pricingInfo, productStage) should be set to null since they're replaced by pillars.
  </action>
  <verify>Run `npx svelte-check` to verify types compile. Confirm schema.ts has contentPillars table and relation. Confirm +page.server.ts loads pillars and handles pillar save for personal_brand type.</verify>
  <done>contentPillars table defined in schema with proper FK and relation. Server load returns productType + pillars. Form action saves pillars for personal_brand, saves product details for other types.</done>
</task>

<task type="auto">
  <name>Task 2: Conditional Deep Brief Section 1 UI with pillar card editor + i18n</name>
  <files>
    src/routes/dashboard/onboarding/deep-brief/+page.svelte
    messages/en.json
    messages/es.json
  </files>
  <action>
    1. In `messages/en.json` and `messages/es.json`, add these keys (after existing `onb_brief_section_product`):
       - `onb_brief_section_pillars`: "Content Pillars" / "Pilares de contenido"
       - `onb_brief_pillars_subtitle`: "Define the topics you post about — the AI will distribute content across them" / "Define los temas sobre los que publicas — la IA distribuira contenido entre ellos"
       - `onb_brief_pillar_name`: "Topic name" / "Nombre del tema"
       - `onb_brief_pillar_name_placeholder`: "e.g., Leadership Lessons" / "ej., Lecciones de liderazgo"
       - `onb_brief_pillar_desc`: "Short description" / "Descripcion breve"
       - `onb_brief_pillar_desc_placeholder`: "What you'll share about this topic" / "Que compartiras sobre este tema"
       - `onb_brief_pillar_add`: "Add pillar" / "Agregar pilar"
       - `onb_brief_pillar_remove`: "Remove" / "Eliminar"
       - `onb_brief_pillar_max`: "Maximum 5 pillars" / "Maximo 5 pilares"
       - `onb_brief_pillar_suggest_journey`: "Personal Journey" / "Recorrido personal"
       - `onb_brief_pillar_suggest_insights`: "Industry Insights" / "Perspectivas del sector"
       - `onb_brief_pillar_suggest_promo`: "Product Promotion" / "Promocion de producto"
       - `onb_brief_pillar_suggest_tips`: "Tips & How-tos" / "Consejos y guias"
       - `onb_brief_pillar_suggest_stories`: "Behind the Scenes" / "Detras de escena"
       - `onb_brief_pillar_suggestions_label`: "Quick add:" / "Agregar rapido:"

    2. In `+page.svelte`:
       - Read `data.productType` and `data.pillars` from PageData.
       - Add a `$state` array for pillars: `let pillars = $state<Array<{ name: string; description: string }>>(data.pillars?.map(p => ({ name: p.name, description: p.description ?? '' })) ?? [])`. If empty, pre-populate with one empty pillar `[{ name: '', description: '' }]` so the user sees at least one card.
       - Add a hidden input `<input type="hidden" name="pillars" value={JSON.stringify(pillars)} />` alongside the other hidden inputs.
       - Define pillar suggestion presets as an array: `const pillarSuggestions = [ { name: () => m.onb_brief_pillar_suggest_journey(), desc: '' }, { name: () => m.onb_brief_pillar_suggest_insights(), desc: '' }, { name: () => m.onb_brief_pillar_suggest_promo(), desc: '' }, { name: () => m.onb_brief_pillar_suggest_tips(), desc: '' }, { name: () => m.onb_brief_pillar_suggest_stories(), desc: '' } ]`.
       - Replace Section 1 (`<!-- Section 1: Product Details -->`) with a conditional block:
         ```
         {#if data.productType === 'personal_brand'}
           <!-- Section 1: Content Pillars -->
           (pillar card editor UI)
         {:else}
           <!-- Section 1: Product Details (existing code, unchanged) -->
         {/if}
         ```
       - The pillar editor section should:
         - Use the same section card styling (rounded-2xl, bg-surface, border-subtle) and numbered badge (1, electric color).
         - Show section title via `m.onb_brief_section_pillars()` and subtitle `m.onb_brief_pillars_subtitle()` in text-dim.
         - Render each pillar as a card with: name text input + description textarea, and a remove button (X icon or Trash2) in the top-right corner. Cards use rounded-xl, bg-surface-alt or input-bg background, border-subtle border.
         - "Add pillar" button at the bottom (disabled when pillars.length >= 5, show max message). Style: dashed border, text-muted color, hover:border-electric.
         - Quick-add suggestions row below the cards: small pills/buttons for each preset. Clicking a suggestion adds a new pillar with that name pre-filled (only if < 5 pillars and not already added). Style: small rounded-lg pills with bg-surface-alt, hover:bg-electric-glow.
         - Minimum 1 pillar enforced: don't allow removing the last pillar (disable remove button or hide it when pillars.length === 1).
       - IMPORTANT: The existing Product Details section code must remain intact inside the {:else} branch. Do NOT modify, remove, or restructure any existing fields (problemSolved, keyFeatures, differentiator, pricingInfo, productStage).
  </action>
  <verify>Run `npx svelte-check` to verify no type errors. Visually inspect: check that the pillar editor shows for personal_brand and Product Details shows for other types. Verify all i18n keys exist in both en.json and es.json.</verify>
  <done>Deep Brief Section 1 conditionally shows Content Pillars editor (card-based, add/remove, suggestions, 1-5 limit) for personal_brand type, and the original Product Details for product/service types. All text uses Paraglide i18n in both languages.</done>
</task>

</tasks>

<verification>
- `npx svelte-check` passes with no errors
- schema.ts contains contentPillars table with id, productId (FK), name, description, sortOrder, createdAt
- productsRelations includes contentPillars: many(contentPillars)
- +page.server.ts load returns productType and pillars array
- +page.server.ts action saves/deletes pillars for personal_brand, saves product details for others
- +page.svelte conditionally renders pillar editor vs product details based on productType
- Pillar editor has: card UI, name+description fields, add/remove, max 5 limit, suggestions
- messages/en.json and messages/es.json both have all onb_brief_pillar_* keys
</verification>

<success_criteria>
- Personal Brand user visiting Deep Brief sees a Content Pillars card editor in Section 1
- Product/Service user visiting Deep Brief sees the original Product Details form unchanged
- User can add up to 5 pillars and remove down to 1
- Quick-add suggestions pre-fill pillar names
- Saving the form persists pillars to contentPillars table
- Returning to the page loads existing pillars from the database
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pillars/07-01-SUMMARY.md`
</output>
