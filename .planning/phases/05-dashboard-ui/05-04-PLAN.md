---
phase: 05-dashboard-ui
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/routes/dashboard/editor/+page.svelte
  - src/routes/dashboard/publishing/+page.svelte
  - src/lib/components/dashboard/kanban-column.svelte
  - src/lib/components/dashboard/post-card.svelte
  - src/lib/components/dashboard/ai-panel.svelte
  - src/lib/data/mock-editor.ts
  - src/lib/data/mock-publishing.ts
  - messages/en.json
  - messages/es.json
autonomous: true

must_haves:
  truths:
    - "User sees a 3-panel content editor layout: brief form (left), content preview (center), variants & history (right)"
    - "User sees a kanban board with 4 columns: Drafts, Review, Scheduled, Published"
    - "User sees post cards within kanban columns with platform badges, preview text, and status"
    - "User sees an AI assistant panel on the Publishing page"
    - "All data comes from typed mock data files"
  artifacts:
    - path: "src/routes/dashboard/editor/+page.svelte"
      provides: "3-panel Content Editor page"
      min_lines: 50
    - path: "src/routes/dashboard/publishing/+page.svelte"
      provides: "Kanban Publishing Hub page with AI panel"
      min_lines: 50
    - path: "src/lib/components/dashboard/kanban-column.svelte"
      provides: "Single kanban column container with header and card slots"
      min_lines: 25
    - path: "src/lib/components/dashboard/post-card.svelte"
      provides: "Post card for kanban board with preview, platform, status"
      min_lines: 30
    - path: "src/lib/components/dashboard/ai-panel.svelte"
      provides: "AI assistant sidebar panel with suggestions and chat-like UI"
      min_lines: 40
    - path: "src/lib/data/mock-editor.ts"
      provides: "Typed mock data for editor: brief, content, variants"
      exports: ["briefData", "contentPreview", "contentVariants", "editHistory"]
    - path: "src/lib/data/mock-publishing.ts"
      provides: "Typed mock data for kanban columns and AI suggestions"
      exports: ["kanbanColumns", "aiSuggestions", "publishingStats"]
  key_links:
    - from: "src/routes/dashboard/editor/+page.svelte"
      to: "src/lib/data/mock-editor.ts"
      via: "import mock data"
      pattern: "import.*mock-editor"
    - from: "src/routes/dashboard/publishing/+page.svelte"
      to: "src/lib/data/mock-publishing.ts"
      via: "import mock data"
      pattern: "import.*mock-publishing"
    - from: "src/routes/dashboard/publishing/+page.svelte"
      to: "src/lib/components/dashboard/kanban-column.svelte"
      via: "import KanbanColumn"
      pattern: "import KanbanColumn"
    - from: "src/lib/components/dashboard/kanban-column.svelte"
      to: "src/lib/components/dashboard/post-card.svelte"
      via: "renders PostCard children"
      pattern: "PostCard"
---

<objective>
Build the Content Editor (variant-3) and Publishing Hub (variant-5) dashboard pages.

Purpose: The Content Editor is the creation workspace where users compose and refine content with AI assistance. The Publishing Hub is the pipeline view where users manage content through the drafts-to-published workflow. These are the two most complex dashboard views with multi-panel layouts.

Output: Two fully styled dashboard pages at `/dashboard/editor` and `/dashboard/publishing`, matching their design variants, using typed mock data.
</objective>

<execution_context>
@/home/diego/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-ui/05-RESEARCH.md
@.planning/phases/05-dashboard-ui/05-01-SUMMARY.md

@design-variants/dashboard/variant-3.html
@design-variants/dashboard/variant-5.html
@src/app.css
@src/lib/styles/theme.css
@src/lib/styles/dashboard.css
@messages/en.json
@messages/es.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content Editor page (variant-3) with 3-panel layout</name>
  <files>
    src/lib/data/mock-editor.ts
    src/routes/dashboard/editor/+page.svelte
    messages/en.json
    messages/es.json
  </files>
  <action>
**Mock data** (`src/lib/data/mock-editor.ts`):

Types:
```typescript
export interface BriefField {
  id: string;
  label: string;
  type: 'text' | 'textarea' | 'select' | 'tags';
  value: string;
  placeholder?: string;
  options?: string[]; // for select type
}

export interface ContentPreview {
  id: string;
  platform: string;
  content: string;
  hashtags: string[];
  characterCount: number;
  characterLimit: number;
  imageUrl?: string;
  aiScore: number; // 0-100 quality score
  hookScore: number;
  ctaScore: number;
  readabilityScore: number;
}

export interface ContentVariant {
  id: string;
  label: string;
  preview: string; // First 80 chars of content
  score: number;
  isSelected: boolean;
}

export interface EditHistoryItem {
  id: string;
  action: string;
  timestamp: string;
  user: string; // 'AI' or 'You'
}
```

Data:
- Brief fields (6 fields):
  1. "Campaign" (select, value: "Product Launch 2024", options: ["Product Launch 2024", "Thought Leadership", "Customer Stories"])
  2. "Topic" (text, value: "5 AI Tools Every Startup Needs in 2024")
  3. "Key Message" (textarea, value: "AI tools are no longer optional for startups...")
  4. "Target Audience" (text, value: "Startup founders, CTOs, indie hackers")
  5. "Content Type" (select, value: "Carousel Post", options: ["Single Image", "Carousel Post", "Text Only", "Video"])
  6. "Tags" (tags, value: "ai, startups, tools, productivity")

- Content preview:
  - platform: "LinkedIn"
  - content: Multi-paragraph LinkedIn post text (~300 chars). Something like: "Stop wasting 10 hours a week on marketing.\n\nI tested 47 AI tools over 6 months. Here are the 5 that actually moved the needle for our startup:\n\n1. Content Strategy — BriefAgent generates your entire 2-week plan from a single brief\n2. Visual Design — Midjourney creates brand-consistent visuals in seconds\n3. Copy Refinement — Claude polishes your copy while keeping your voice\n4. Scheduling — Buffer handles multi-platform posting on autopilot\n5. Analytics — Metricool shows what's actually working\n\nThe result? 3x more content. 40% higher engagement. Zero extra hires.\n\nWhich AI tool has been your biggest game-changer? Drop it below."
  - hashtags: ["#AITools", "#StartupGrowth", "#ContentMarketing", "#MarketingAutomation", "#AI"]
  - characterCount: 612
  - characterLimit: 3000
  - aiScore: 92
  - hookScore: 95
  - ctaScore: 88
  - readabilityScore: 91

- 3 content variants:
  1. "Professional Tone" (preview first 80 chars, score 92, selected)
  2. "Casual / Conversational" (preview first 80 chars, score 87, not selected)
  3. "Data-Heavy" (preview first 80 chars, score 84, not selected)

- Edit history (5 items):
  1. "Generated initial content" (AI, 2 min ago)
  2. "Adjusted hook line" (You, 5 min ago)
  3. "Regenerated with casual tone" (AI, 8 min ago)
  4. "Added hashtag suggestions" (AI, 10 min ago)
  5. "Created from brief" (AI, 15 min ago)

**Content Editor page** (`src/routes/dashboard/editor/+page.svelte`):

This is the most complex page. Use a 3-panel layout matching variant-3.

Layout: `flex h-full` filling the entire dashboard content area (the page itself must be full height, no padding — override default layout padding with a negative margin or set the page to fill).

**Left panel** (~320px, border-r, bg --bg-surface, overflow-y-auto, flex-shrink-0):
- Header: "Brief" title + "Generate" button (bg --c-electric, text white, rounded-lg, flex items-center gap-2, Sparkles icon from lucide-svelte)
- Form fields: Render each BriefField as appropriate input:
  - text: `<input>` with bg --input-bg, border --border-subtle, rounded-lg, px-3 py-2.5, text-sm, focus:border --c-electric
  - textarea: `<textarea>` with same styling, min-h-[80px]
  - select: `<select>` with same styling + ChevronDown icon
  - tags: Display as row of small pills (bg --bg-sidebar-active, text --c-electric, rounded-full, px-2.5 py-1, text-xs)
- Each field: label (text-xs font-bold uppercase tracking-wider --text-muted, mb-1.5) + input
- Fields stacked with gap-4, px-5 py-4

**Center panel** (flex-1, overflow-y-auto, bg --bg-page):
- Header row: Platform selector pill ("LinkedIn" active, others dim) + character count ("612 / 3,000" in font-mono text-xs)
- Content preview area: Large text area showing the full content. Styled like a social media post preview:
  - White/dark card (rounded-xl, border, p-6, bg --bg-surface)
  - Author row: avatar + "BriefAgent" name + "Just now" timestamp
  - Content text with line breaks preserved (whitespace-pre-line)
  - Hashtags row at bottom: each hashtag in --c-electric text
  - Image placeholder area if applicable (gray rounded box with Image icon)
- Score badges row below preview: 4 small score indicators
  - AI Score: 92 (--c-electric)
  - Hook: 95 (--positive)
  - CTA: 88 (--c-secondary)
  - Readability: 91 (--positive)
  - Each: small rounded-lg pill, bg color at 10%, text color, font-mono text-xs

**Right panel** (~280px, border-l, bg --bg-surface, flex-shrink-0, overflow-y-auto):
- **Variants section** (top half):
  - "Variants" title + "Generate New" button (text-xs, text --c-electric)
  - 3 variant cards stacked: each with label, preview text (truncated 2 lines), score badge, selected state (border --c-electric if selected, --border-subtle if not)
  - Click to select (visual only, use `$state` for selectedVariant)
- **History section** (bottom half, border-t):
  - "History" title
  - Timeline list: each entry with dot (AI=--c-electric, You=--c-secondary), action text, timestamp (font-mono text-xs --text-muted)

Total: The page itself composes the 3 panels inline. Since the layout is unique to this page, it does NOT need to be extracted into separate component files — but keep total lines under 180 by using `{#each}` blocks efficiently and keeping markup concise. If approaching limit, extract left-panel form and right-panel sections into small child components within a `dashboard/editor/` directory (NOT needed if under 180 lines).

IMPORTANT: This page needs to fill the full height of the content area. The dashboard layout's `<main>` has `overflow-y-auto` and `flex-1`. This page should set itself to `h-full` and hide the content padding. Add a class or style to override: the page itself renders `<div class="flex h-full -m-8">` (negative margin to cancel the 2rem padding of the main area in the dashboard layout). Or better: check if the dashboard layout can conditionally remove padding. Simplest approach: use `<div class="flex h-full absolute inset-0">` or just accept the padding and use `<div class="flex -m-8 h-[calc(100%+4rem)]">` to expand into the padding area.

**i18n keys**:

en.json:
```json
"dash_editor_title": "Content Editor",
"dash_editor_brief": "Brief",
"dash_editor_generate": "Generate",
"dash_editor_generate_new": "Generate New",
"dash_editor_variants": "Variants",
"dash_editor_history": "History",
"dash_editor_score_ai": "AI Score",
"dash_editor_score_hook": "Hook",
"dash_editor_score_cta": "CTA",
"dash_editor_score_readability": "Readability",
"dash_editor_char_count": "{count} / {limit}",
"dash_editor_just_now": "Just now",
"dash_editor_campaign": "Campaign",
"dash_editor_topic": "Topic",
"dash_editor_key_message": "Key Message",
"dash_editor_target_audience": "Target Audience",
"dash_editor_content_type": "Content Type",
"dash_editor_tags": "Tags",
"dash_editor_selected": "Selected"
```

es.json:
```json
"dash_editor_title": "Editor de Contenido",
"dash_editor_brief": "Brief",
"dash_editor_generate": "Generar",
"dash_editor_generate_new": "Generar Nuevo",
"dash_editor_variants": "Variantes",
"dash_editor_history": "Historial",
"dash_editor_score_ai": "Puntaje IA",
"dash_editor_score_hook": "Gancho",
"dash_editor_score_cta": "CTA",
"dash_editor_score_readability": "Legibilidad",
"dash_editor_char_count": "{count} / {limit}",
"dash_editor_just_now": "Ahora mismo",
"dash_editor_campaign": "Campana",
"dash_editor_topic": "Tema",
"dash_editor_key_message": "Mensaje Clave",
"dash_editor_target_audience": "Audiencia Objetivo",
"dash_editor_content_type": "Tipo de Contenido",
"dash_editor_tags": "Etiquetas",
"dash_editor_selected": "Seleccionado"
```

Use proper accented characters in actual file.
  </action>
  <verify>
    - `pnpm dev` — navigate to `/dashboard/editor` via sidebar
    - 3-panel layout visible: brief form (left), content preview (center), variants+history (right)
    - Brief form shows 6 fields with labels, proper input styling
    - Content preview shows formatted LinkedIn post with hashtags and scores
    - Variants panel shows 3 options with one selected
    - History shows 5 timeline entries
    - Sidebar "Content" nav item highlighted
    - `npx svelte-check` passes
    - `pnpm build` succeeds
  </verify>
  <done>
    - Content Editor page at `/dashboard/editor` renders 3-panel layout matching variant-3
    - Left: brief form with 6 styled fields
    - Center: LinkedIn post preview with character count and 4 AI scores
    - Right: 3 variants (selectable) + edit history timeline
    - mock-editor.ts exports typed data
    - ~19 new i18n keys in en/es
    - Page fills full height of dashboard content area
  </done>
</task>

<task type="auto">
  <name>Task 2: Publishing Hub page (variant-5) with kanban board and AI panel</name>
  <files>
    src/lib/data/mock-publishing.ts
    src/lib/components/dashboard/kanban-column.svelte
    src/lib/components/dashboard/post-card.svelte
    src/lib/components/dashboard/ai-panel.svelte
    src/routes/dashboard/publishing/+page.svelte
    messages/en.json
    messages/es.json
  </files>
  <action>
**Mock data** (`src/lib/data/mock-publishing.ts`):

Types:
```typescript
export type PostStatus = 'draft' | 'review' | 'scheduled' | 'published';

export interface PostCard {
  id: string;
  title: string;
  preview: string; // First ~60 chars of content
  platform: string;
  type: 'image' | 'carousel' | 'text' | 'video';
  status: PostStatus;
  scheduledTime?: string;
  publishedTime?: string;
  engagement?: { likes: number; comments: number; shares: number };
  authorAvatar: string; // Initials for avatar
}

export interface KanbanColumn {
  id: PostStatus;
  label: string;
  count: number;
  posts: PostCard[];
}

export interface AiSuggestion {
  id: string;
  type: 'optimization' | 'timing' | 'content' | 'warning';
  title: string;
  description: string;
  action?: string; // Button label
}

export interface PublishingStat {
  label: string;
  value: string;
  change?: string;
}
```

Data:
- 4 kanban columns:
  1. Drafts (3 posts): "AI Tools Roundup" (image, LinkedIn), "Design Trends 2024" (carousel, Instagram), "Quick Poll: Favorite Tool" (text, Twitter/X)
  2. Review (2 posts): "Customer Story: Acme Corp" (image, LinkedIn), "Product Update v2.1" (carousel, LinkedIn)
  3. Scheduled (3 posts): "5 Growth Hacks" (carousel, LinkedIn, Tomorrow 2PM), "Behind the Scenes" (image, Instagram, Tomorrow 4:30PM), "Weekly Tips Thread" (text, Twitter/X, Wed 9AM)
  4. Published (4 posts): "Startup Toolkit Guide" (carousel, LinkedIn, 142 likes, 23 comments, 18 shares), "Team Culture Post" (image, Instagram, 89 likes, 12 comments), "Product Demo" (video, LinkedIn, 234 likes, 45 comments, 31 shares), "Industry News Take" (text, Twitter/X, 67 likes, 8 comments)

- AI suggestions (4):
  1. optimization: "Optimize posting time" / "Your LinkedIn posts perform 40% better when published between 8-10 AM" / "Reschedule"
  2. timing: "Content gap detected" / "You haven't posted on Instagram in 5 days. Engagement drops after 3-day gaps." / "Create Post"
  3. content: "Trending topic alert" / "AI in Marketing is trending in your niche. Consider creating a thought piece." / "Generate Brief"
  4. warning: "Review overdue" / "2 posts have been in review for over 48 hours." / "Review Now"

- Publishing stats (3):
  1. "Published This Week" / "8" / "+3 vs last week"
  2. "Avg. Engagement" / "4.2%" / "+0.8%"
  3. "Queue Health" / "Good" / "12 posts scheduled"

**Kanban column component** (`src/lib/components/dashboard/kanban-column.svelte`):

Props: `{ label: string; count: number; status: PostStatus; children: Snippet }`

Design (from variant-5):
- Column container: flex-1, min-w-[260px], max-w-[320px], flex flex-col, rounded-xl, bg --bg-surface-alt (or slightly transparent)
- Header: flex items-center justify-between, px-4 py-3
  - Left: status dot (8px circle, color by status: draft=--text-muted, review=--c-secondary, scheduled=--c-electric, published=--positive) + label (text-sm font-bold --text-main) + count badge (text-xs bg --border-subtle rounded-full px-2 py-0.5 --text-dim)
  - Right: "+" button (w-7 h-7, rounded-lg, border --border-subtle, hover bg --bg-sidebar-hover, Plus icon 14px)
- Body: flex-1, overflow-y-auto, px-3 pb-3, flex flex-col gap-2.5
  - Render `{@render children()}` for post cards
- Subtle top border colored by status (3px, matching dot color)

**Post card component** (`src/lib/components/dashboard/post-card.svelte`):

Props: `{ post: PostCard }`

Design (from variant-5):
- Card: rounded-xl, border --border-subtle, bg --bg-surface, p-3.5, transition hover:-translate-y-0.5 hover:border-[var(--border-hover)], cursor-pointer
- Top row: platform badge (text-[0.65rem] font-bold uppercase tracking-wider, bg by platform: LinkedIn=--c-electric/10, Instagram=--c-tertiary/10, Twitter=--c-secondary/10, color matching) + type indicator (text-[0.65rem] --text-muted, capitalize)
- Title: text-sm font-semibold --text-main, mt-2, line-clamp-1
- Preview: text-xs --text-dim, mt-1, line-clamp-2 (truncated with ellipsis)
- Bottom row (mt-3): conditional by status:
  - draft: "Edit" and "Submit" small buttons (text-[0.7rem])
  - review: "Approve" button (text --positive) + "Reject" button (text --negative)
  - scheduled: Clock icon + scheduled time (font-mono text-xs --c-electric)
  - published: engagement stats (likes/comments/shares icons with counts, text-xs --text-dim, gap-3)
- Subtle shadow: var(--card-shadow)

**AI panel component** (`src/lib/components/dashboard/ai-panel.svelte`):

Props: `{ suggestions: AiSuggestion[]; stats: PublishingStat[] }`

Design (from variant-5):
- Panel: w-[300px], border-l --border-subtle, bg --bg-surface, flex flex-col, flex-shrink-0
- Header: "AI Assistant" title + sparkle icon (Sparkles from lucide-svelte), px-5 py-4, border-b
- Stats section: 3 stat items in a column, each with label (text-xs --text-dim), value (text-lg font-extrabold --text-main), change (text-xs, positive=--positive, neutral=--text-dim). Separated by border-b.
- Suggestions section: "Suggestions" subtitle + list of suggestion cards:
  - Each card: rounded-lg, border --border-subtle, p-3, bg --bg-surface-alt
  - Type icon: optimization=Zap, timing=Clock, content=FileText, warning=AlertTriangle (from lucide-svelte). Color: optimization=--c-electric, timing=--c-secondary, content=--positive, warning=--negative
  - Title: text-sm font-semibold --text-main
  - Description: text-xs --text-dim, mt-1
  - Action button (if present): text-xs font-bold text --c-electric, mt-2, hover underline
- Bottom: "Ask AI..." input field (rounded-lg, bg --input-bg, border --border-subtle, px-3 py-2.5, text-sm, placeholder text --text-muted, Send icon button on right)

**Publishing Hub page** (`src/routes/dashboard/publishing/+page.svelte`):

Layout (matching variant-5):
```
<svelte:head><title>{m.dash_meta_title_publishing()}</title></svelte:head>

<div class="flex h-full -m-8"> (or similar full-height override)

  Main area (flex-1, overflow-hidden, flex flex-col):
    1. Header row (px-6 py-4): "Publishing Hub" title + filter buttons (All, LinkedIn, Instagram, Twitter) + "New Post" button (bg --c-electric)
    2. Kanban board (flex-1, overflow-x-auto, px-6 pb-4):
       <div class="flex gap-4 h-full min-w-max">
         4 KanbanColumn components, each containing PostCard components
       </div>

  Right panel: AiPanel component (fixed width, full height)

</div>
```

The kanban columns should scroll horizontally if viewport is narrow. Each column scrolls vertically for its cards.

IMPORTANT: Same full-height handling as the editor page. The page fills the entire content area with no padding.

**i18n keys**:

en.json:
```json
"dash_publishing_title": "Publishing Hub",
"dash_publishing_new_post": "New Post",
"dash_publishing_filter_all": "All",
"dash_publishing_col_drafts": "Drafts",
"dash_publishing_col_review": "In Review",
"dash_publishing_col_scheduled": "Scheduled",
"dash_publishing_col_published": "Published",
"dash_publishing_ai_assistant": "AI Assistant",
"dash_publishing_ai_suggestions": "Suggestions",
"dash_publishing_ai_ask": "Ask AI...",
"dash_publishing_edit": "Edit",
"dash_publishing_submit": "Submit",
"dash_publishing_approve": "Approve",
"dash_publishing_reject": "Reject",
"dash_publishing_stat_published_week": "Published This Week",
"dash_publishing_stat_avg_engagement": "Avg. Engagement",
"dash_publishing_stat_queue_health": "Queue Health"
```

es.json:
```json
"dash_publishing_title": "Centro de Publicacion",
"dash_publishing_new_post": "Nueva Publicacion",
"dash_publishing_filter_all": "Todos",
"dash_publishing_col_drafts": "Borradores",
"dash_publishing_col_review": "En Revision",
"dash_publishing_col_scheduled": "Programados",
"dash_publishing_col_published": "Publicados",
"dash_publishing_ai_assistant": "Asistente IA",
"dash_publishing_ai_suggestions": "Sugerencias",
"dash_publishing_ai_ask": "Preguntar a IA...",
"dash_publishing_edit": "Editar",
"dash_publishing_submit": "Enviar",
"dash_publishing_approve": "Aprobar",
"dash_publishing_reject": "Rechazar",
"dash_publishing_stat_published_week": "Publicados Esta Semana",
"dash_publishing_stat_avg_engagement": "Interaccion Promedio",
"dash_publishing_stat_queue_health": "Salud de Cola"
```

Use proper accented characters in actual file.
  </action>
  <verify>
    - `pnpm dev` — navigate to `/dashboard/publishing` via sidebar
    - 4 kanban columns visible with correct headers and colored top borders
    - Post cards render within columns with platform badges, titles, previews
    - Draft cards have Edit/Submit buttons, published cards show engagement stats
    - AI panel on right shows stats, 4 suggestions, and input field
    - Columns scroll vertically, board scrolls horizontally if needed
    - Navigate to `/dashboard/editor` — 3-panel layout fills full height
    - Sidebar highlights correct items on both pages
    - Theme toggle works on all elements
    - `npx svelte-check` passes
    - `pnpm build` succeeds
  </verify>
  <done>
    - Content Editor at `/dashboard/editor`: 3-panel layout with brief form, content preview, variants+history
    - Publishing Hub at `/dashboard/publishing`: 4-column kanban + AI assistant panel
    - 5 new components: kanban-column, post-card, ai-panel + 2 pages
    - 2 typed mock data files (mock-editor.ts, mock-publishing.ts)
    - ~36 new i18n keys in en/es
    - Both pages fill full height of dashboard content area
    - Post cards show contextual actions based on status
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. `/dashboard/editor` renders 3-panel layout: brief form, content preview, variants + history
3. `/dashboard/publishing` renders 4-column kanban board + AI assistant panel
4. All 5 dashboard views navigable via sidebar: overview, calendar, editor, brand, publishing
5. Active sidebar highlighting works on all 5 pages
6. Theme toggle works across all pages and components
7. `npx svelte-check` passes
8. All page files under 180 lines, component files under 200 lines
</verification>

<success_criteria>
- Content Editor page matches variant-3: 3-panel layout, brief form, LinkedIn preview with scores, variants, history
- Publishing Hub page matches variant-5: 4-column kanban, post cards with status-specific actions, AI assistant panel
- All 5 dashboard views complete and navigable
- 5 new reusable components: kanban-column, post-card, ai-panel + 2 pages
- Typed mock data in mock-editor.ts and mock-publishing.ts
- ~36 new i18n keys in both languages
- Full-height layouts for editor and publishing pages
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-ui/05-04-SUMMARY.md`
</output>
