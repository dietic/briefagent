---
phase: 08-platform-selection-per-pillar
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/lib/server/ai/pipeline/brief-assembler.ts
  - src/lib/server/ai/schemas/plan.ts
  - src/lib/server/ai/prompts/plan-system.ts
  - src/lib/server/ai/prompts/copy-system.ts
  - src/lib/server/ai/prompts/copy-generator.ts
  - src/lib/server/ai/prompts/image-system.ts
  - src/lib/server/ai/pipeline/image-generator.ts
  - src/lib/server/ai/pipeline/copy-generator.ts
  - src/lib/server/ai/pipeline/post-generator.ts
autonomous: true
requirements: [SC-04, SC-05]

must_haves:
  truths:
    - "AI content plan generation assigns the correct platform to each post based on its pillar's platform setting"
    - "Copy generation uses platform-specific rules (char limits, hashtag rules, tone) from PLATFORM_SPECS"
    - "Image generation resizes to platform-specific dimensions (1200x1200 for LinkedIn, 1200x675 for X)"
    - "Posts without a platform assignment (null) default to LinkedIn behavior for backward compatibility"
    - "X/Twitter posts prefer text_only postType in the plan prompt to avoid unnecessary image generation"
  artifacts:
    - path: "src/lib/server/ai/pipeline/brief-assembler.ts"
      provides: "Platform field in contentPillars array of AssembledBrief"
      contains: "platform: string | null"
    - path: "src/lib/server/ai/schemas/plan.ts"
      provides: "Multi-platform Zod schema for post slots"
      contains: "z.enum(['linkedin', 'x'])"
    - path: "src/lib/server/ai/prompts/plan-system.ts"
      provides: "Platform-aware plan system and user prompts"
      contains: "getPlatformSpec"
    - path: "src/lib/server/ai/prompts/copy-system.ts"
      provides: "Platform-specific copy rules injection"
      contains: "getPlatformSpec"
    - path: "src/lib/server/ai/pipeline/image-generator.ts"
      provides: "Platform-specific image resize dimensions"
      contains: "spec.imageSize"
  key_links:
    - from: "src/lib/server/ai/pipeline/brief-assembler.ts"
      to: "src/lib/server/ai/prompts/plan-system.ts"
      via: "AssembledBrief.contentPillars[].platform passed to buildPlanUserPrompt"
      pattern: "brief\\.contentPillars"
    - from: "src/lib/server/ai/prompts/plan-system.ts"
      to: "src/lib/server/ai/platform-specs.ts"
      via: "getPlatformSpec for platform-aware prompt text"
      pattern: "getPlatformSpec"
    - from: "src/lib/server/ai/prompts/copy-system.ts"
      to: "src/lib/server/ai/platform-specs.ts"
      via: "getPlatformSpec for copy rules injection"
      pattern: "getPlatformSpec"
    - from: "src/lib/server/ai/pipeline/image-generator.ts"
      to: "src/lib/server/ai/platform-specs.ts"
      via: "getPlatformSpec for resize dimensions"
      pattern: "spec\\.imageSize"
    - from: "src/lib/server/ai/pipeline/post-generator.ts"
      to: "src/lib/server/ai/pipeline/copy-generator.ts"
      via: "passes post.platform to copy generator"
      pattern: "post\\.platform"
---

<objective>
Make the entire AI content generation pipeline platform-aware. The brief assembler includes pillar platform data, prompts inject platform-specific rules, images resize to platform dimensions, and copy respects character limits. Posts without a platform default to LinkedIn.

Purpose: Deliver platform-specific content quality — X posts are concise (280 chars), LinkedIn posts are long-form (1200-1800 chars), images match each platform's aspect ratio.
Output: Updated pipeline files producing platform-correct content for LinkedIn and X.
</objective>

<execution_context>
@/home/diego/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-platform-selection-per-pillar/08-RESEARCH.md
@.planning/phases/08-platform-selection-per-pillar/08-01-SUMMARY.md
@src/lib/server/ai/platform-specs.ts
@src/lib/server/ai/pipeline/brief-assembler.ts
@src/lib/server/ai/schemas/plan.ts
@src/lib/server/ai/prompts/plan-system.ts
@src/lib/server/ai/prompts/copy-system.ts
@src/lib/server/ai/pipeline/copy-generator.ts
@src/lib/server/ai/prompts/image-system.ts
@src/lib/server/ai/pipeline/image-generator.ts
@src/lib/server/ai/pipeline/post-generator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brief assembler + plan schema + plan prompts (platform-aware planning)</name>
  <files>
    src/lib/server/ai/pipeline/brief-assembler.ts
    src/lib/server/ai/schemas/plan.ts
    src/lib/server/ai/prompts/plan-system.ts
  </files>
  <action>
1. **Brief assembler** (`src/lib/server/ai/pipeline/brief-assembler.ts`):
   - Add `platform: string | null` to the `contentPillars` array type in the `AssembledBrief` interface
   - In the `assembleBrief()` function, include `platform: p.platform` in the pillars mapping

2. **Plan Zod schema** (`src/lib/server/ai/schemas/plan.ts`):
   - Change `platform: z.literal('linkedin')` to `platform: z.enum(['linkedin', 'x'])` in the `postSlotSchema`
   - This allows the AI to output posts for either platform via structured output validation

3. **Plan system prompt** (`src/lib/server/ai/prompts/plan-system.ts`):
   - Import `getPlatformSpec` from `'../platform-specs'`
   - Update `buildPlanSystemPrompt()`:
     - Change "expert LinkedIn content strategist" to "expert social media content strategist"
     - Change "optimal LinkedIn engagement times" to "optimal engagement times per platform"
     - Add rule: "Each post's `platform` field MUST match its assigned pillar's platform. Pillars without a platform default to linkedin."
     - Add rule: "For X/Twitter posts, prefer text_only post type unless the topic strongly benefits from a visual. X is a text-first platform."
     - Keep all existing rules intact
   - Update `buildPlanUserPrompt()`:
     - In the content pillars section (the `if (brief.contentPillars.length > 0)` branch), add platform labels to each pillar line:
       ```typescript
       const pillarLines = brief.contentPillars.map(
         (p, i) => {
           const platformLabel = p.platform
             ? `[Platform: ${getPlatformSpec(p.platform).displayName}]`
             : '[Platform: LinkedIn (default)]';
           return `${i + 1}. **${p.name}**${p.description ? `: ${p.description}` : ''} ${platformLabel}`;
         }
       );
       ```
     - After the pillar list, add: "IMPORTANT: Each post's platform field MUST match its pillar's platform. If a pillar has no platform, use 'linkedin'."
  </action>
  <verify>
    - `pnpm build` succeeds
    - `AssembledBrief` type includes `platform: string | null` in contentPillars
    - `postSlotSchema` accepts both 'linkedin' and 'x' values for platform
    - Plan system prompt no longer hardcodes "LinkedIn content strategist"
    - Plan user prompt includes platform label per pillar
  </verify>
  <done>
    Brief assembler carries pillar platform data. Plan schema accepts multi-platform posts. Plan prompts instruct the AI to assign platforms per pillar and prefer text_only for X posts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Copy prompts + image prompts + generators (platform-aware generation)</name>
  <files>
    src/lib/server/ai/prompts/copy-system.ts
    src/lib/server/ai/pipeline/copy-generator.ts
    src/lib/server/ai/prompts/image-system.ts
    src/lib/server/ai/pipeline/image-generator.ts
    src/lib/server/ai/pipeline/post-generator.ts
  </files>
  <action>
1. **Copy system prompt** (`src/lib/server/ai/prompts/copy-system.ts`):
   - Import `getPlatformSpec` from `'../platform-specs'`
   - Change `buildCopySystemPrompt(brief: AssembledBrief)` signature to `buildCopySystemPrompt(brief: AssembledBrief, platform: string | null)`
   - Get platform spec: `const spec = getPlatformSpec(platform);`
   - Replace hardcoded "LinkedIn content writer" with `${spec.displayName} content writer`
   - Replace the entire "LinkedIn Copy Rules:" block with platform-dynamic rules:
     ```
     ${spec.displayName} Copy Rules:
     1. Scroll-stopping hook as the FIRST line -- create curiosity or challenge assumptions
     2. Value-driven body -- teach, share insight, or tell a story (not just promote)
     3. ${spec.toneGuidelines}
     4. CTA aligned with the main goal (${b.mainGoal ?? 'brand awareness'})
     5. ${spec.hashtagRules}
     6. Optimal post length: ${spec.charRecommended.min}-${spec.charRecommended.max} characters (hard limit: ${spec.charLimit})
     ```
   - Add platform-specific extras conditionally:
     - If linkedin: "Use line breaks for readability -- LinkedIn rewards scannable content\nNever use emojis excessively -- 0-2 max if they add meaning"
     - If x: "Be concise -- every word must earn its place\nConsider thread format for complex topics (but output as single post for now)"
   - Also update `buildCopyUserPrompt()` to accept and include `platform: string`:
     - Change "Write a LinkedIn post" to `Write a ${platformDisplayName} post`
     - Pass the platform display name as a parameter or compute it inline

2. **Copy generator** (`src/lib/server/ai/pipeline/copy-generator.ts`):
   - Add `platform: string | null` parameter to `generatePostCopy()` function signature (after `brief`)
   - Pass `platform` to `buildCopySystemPrompt(brief, platform)`

3. **Image system prompt** (`src/lib/server/ai/prompts/image-system.ts`):
   - Import `getPlatformSpec` from `'../platform-specs'`
   - Add `platform: string | null` parameter to `buildImagePrompt()`
   - Get platform spec: `const spec = getPlatformSpec(platform);`
   - Replace "professional social media image for LinkedIn" with `professional social media image for ${spec.displayName}`
   - Replace "Professional, clean composition suitable for LinkedIn" with `Professional, clean composition suitable for ${spec.displayName} (${spec.imageAspectRatio} aspect ratio)`

4. **Image generator** (`src/lib/server/ai/pipeline/image-generator.ts`):
   - Import `getPlatformSpec` from `'../platform-specs'`
   - Add `platform: string | null` parameter to `generatePostImage()` function signature (after `postId`)
   - Get spec: `const spec = getPlatformSpec(platform);`
   - Replace hardcoded `sharp(buffer).resize(1200, 1200, { fit: 'cover' })` with `sharp(buffer).resize(spec.imageSize.width, spec.imageSize.height, { fit: 'cover' })`
   - Pass `platform` to `buildImagePrompt(postTopic, keyMessage, brandAnalysis, platform)`

5. **Post generator** (`src/lib/server/ai/pipeline/post-generator.ts`):
   - In the copy generation batch loop, pass `post.platform` to `generatePostCopy()`:
     ```typescript
     generatePostCopy(
       { topic: post.topic, contentCategory: post.contentCategory, keyMessage: post.keyMessage },
       brief,
       post.platform  // NEW
     )
     ```
   - In the image generation loop, pass `post.platform` to `generatePostImage()`:
     ```typescript
     const result = await generatePostImage(
       post.topic,
       post.keyMessage,
       brandAnalysis,
       productId,
       post.id,
       post.platform  // NEW
     );
     ```
  </action>
  <verify>
    - `pnpm build` succeeds with no type errors
    - `buildCopySystemPrompt(brief, 'x')` produces prompt with "X (Twitter) content writer" and 200-280 char range
    - `buildCopySystemPrompt(brief, null)` produces prompt with "LinkedIn content writer" and 1200-1800 char range (default)
    - `buildImagePrompt(topic, msg, brand, 'x')` references "X (Twitter)" and "16:9 aspect ratio"
    - `generatePostImage()` with platform='x' would resize to 1200x675, not 1200x1200
    - `generatePostCopy()` accepts 3rd platform parameter
    - `generatePostsForPlan()` passes post.platform through to both copy and image generators
  </verify>
  <done>
    Entire AI pipeline is platform-aware. Copy uses platform-specific char limits, tone, and hashtag rules. Images resize to platform dimensions. Plan prompts assign correct platforms per pillar. Null platform defaults to LinkedIn throughout.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes with zero type errors
2. Brief assembler includes platform in contentPillars
3. Plan schema accepts both 'linkedin' and 'x' for platform field
4. Plan prompts reference pillar platforms and instruct per-pillar platform assignment
5. Copy prompts dynamically inject platform-specific rules (char limits, tone, hashtags)
6. Image generator resizes to platform-specific dimensions
7. Post generator passes platform through the full pipeline
8. Null/missing platform defaults to LinkedIn everywhere
</verification>

<success_criteria>
- AI content plan assigns posts to platforms matching their pillar's platform setting
- Copy generation respects platform char limits (LinkedIn 1200-1800, X 200-280)
- Image generation resizes to platform dimensions (LinkedIn 1200x1200, X 1200x675)
- No platform = LinkedIn default behavior preserved (backward compatible)
- Build succeeds with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-platform-selection-per-pillar/08-02-SUMMARY.md`
</output>
