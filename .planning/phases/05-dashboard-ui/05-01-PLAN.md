---
phase: 05-dashboard-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+layout.svelte
  - src/routes/(landing)/+layout.svelte
  - src/routes/(landing)/+page.svelte
  - src/routes/dashboard/+layout.svelte
  - src/routes/dashboard/+page.svelte
  - src/lib/components/dashboard/sidebar.svelte
  - src/lib/components/dashboard/header.svelte
  - src/lib/styles/dashboard.css
  - src/app.css
  - messages/en.json
  - messages/es.json
autonomous: true

must_haves:
  truths:
    - "Landing page renders identically at / with Navbar and Footer"
    - "Dashboard route /dashboard renders with sidebar and header, no Navbar or Footer"
    - "Sidebar highlights the active route correctly"
    - "Theme toggle works on both landing and dashboard"
    - "New CSS variables (--bg-sidebar, --text-muted, --positive, --negative, --card-shadow, --scrollbar-thumb) respond to light/dark toggle"
  artifacts:
    - path: "src/routes/+layout.svelte"
      provides: "Minimal root layout with ModeWatcher only"
      contains: "ModeWatcher"
    - path: "src/routes/(landing)/+layout.svelte"
      provides: "Landing layout with Navbar + Footer"
      contains: "Navbar"
    - path: "src/routes/(landing)/+page.svelte"
      provides: "Landing page (moved from root)"
      contains: "Hero"
    - path: "src/routes/dashboard/+layout.svelte"
      provides: "Dashboard layout with sidebar + header + main content area"
      contains: "Sidebar"
    - path: "src/lib/components/dashboard/sidebar.svelte"
      provides: "240px sidebar with logo, nav items, user pill"
      min_lines: 60
    - path: "src/lib/components/dashboard/header.svelte"
      provides: "Top header bar with breadcrumb, search, notifications, theme toggle"
      min_lines: 30
    - path: "src/lib/styles/dashboard.css"
      provides: "Dashboard-specific animations, scrollbar styling, utility classes"
      min_lines: 20
    - path: "src/app.css"
      provides: "Extended CSS variables for dashboard"
      contains: "--bg-sidebar"
  key_links:
    - from: "src/routes/dashboard/+layout.svelte"
      to: "src/lib/components/dashboard/sidebar.svelte"
      via: "import Sidebar"
      pattern: "import Sidebar"
    - from: "src/routes/dashboard/+layout.svelte"
      to: "src/lib/components/dashboard/header.svelte"
      via: "import DashboardHeader"
      pattern: "import.*Header"
    - from: "src/lib/components/dashboard/sidebar.svelte"
      to: "$app/stores"
      via: "page store for active route"
      pattern: "\\$page\\.url\\.pathname"
    - from: "src/routes/(landing)/+layout.svelte"
      to: "src/lib/components/landing/navbar.svelte"
      via: "import Navbar"
      pattern: "import Navbar"
---

<objective>
Refactor the root layout into layout groups and build the shared dashboard shell (sidebar, header, CSS variables).

Purpose: The current root layout renders Navbar+Footer on ALL pages. Dashboard pages need a completely different chrome (sidebar + header). This plan creates the layout group architecture and shared dashboard components that all 5 dashboard views will use.

Output: Working layout group system where / shows landing page with Navbar+Footer, and /dashboard shows the dashboard shell with sidebar navigation and header bar. All existing landing page functionality preserved.
</objective>

<execution_context>
@/home/diego/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-ui/05-RESEARCH.md

@src/routes/+layout.svelte
@src/routes/+page.svelte
@src/app.css
@src/lib/styles/theme.css
@src/lib/components/landing/navbar.svelte
@src/lib/components/landing/footer.svelte
@src/lib/components/ui/theme-toggle.svelte
@design-variants/dashboard/variant-1.html
@design-variants/dashboard/variant-5.html
@messages/en.json
@messages/es.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout group refactoring + dashboard CSS variables</name>
  <files>
    src/routes/+layout.svelte
    src/routes/(landing)/+layout.svelte
    src/routes/(landing)/+page.svelte
    src/app.css
    src/lib/styles/dashboard.css
    messages/en.json
    messages/es.json
  </files>
  <action>
**Layout group refactoring:**

1. **Rewrite `src/routes/+layout.svelte`** to be minimal root layout:
   - Keep `import '../app.css'` and `ModeWatcher`
   - Remove Navbar and Footer imports entirely
   - Just render `{@render children()}`

2. **Create `src/routes/(landing)/+layout.svelte`**:
   - Import and render Navbar from `$lib/components/landing/navbar.svelte`
   - Import and render Footer from `$lib/components/landing/footer.svelte`
   - Render `{@render children()}` between them
   - Use `$props()` for children (Svelte 5)

3. **Move `src/routes/+page.svelte` to `src/routes/(landing)/+page.svelte`**:
   - Copy the file content exactly as-is (Hero, Features, HowItWorks, Pricing, Cta imports)
   - Delete the original `src/routes/+page.svelte`
   - The route still resolves to `/` because `(landing)` is a layout group, not a path segment

**CSS variable extensions:**

4. **Add dashboard-specific CSS variables to `src/app.css`** in both `:root` and `:root:where(.dark, .dark *)` blocks. Add AFTER existing variables, within the same blocks:

Light mode (`:root`):
```css
/* Dashboard extensions */
--bg-sidebar: #edf4f7;
--bg-sidebar-active: rgba(8, 145, 178, 0.08);
--bg-sidebar-hover: rgba(0, 0, 0, 0.03);
--text-muted: #94a8b3;
--card-shadow: 0 1px 4px rgba(0, 0, 0, 0.06);
--positive: #059669;
--negative: #dc2626;
--scrollbar-thumb: rgba(0, 0, 0, 0.12);
--input-bg: rgba(0, 0, 0, 0.02);
--surface-raised: #f8fafb;
```

Dark mode (`:root:where(.dark, .dark *)`):
```css
/* Dashboard extensions */
--bg-sidebar: #0a0f13;
--bg-sidebar-active: rgba(6, 182, 212, 0.08);
--bg-sidebar-hover: rgba(255, 255, 255, 0.03);
--text-muted: #3d5563;
--card-shadow: 0 1px 3px rgba(0, 0, 0, 0.4);
--positive: #34d399;
--negative: #f87171;
--scrollbar-thumb: rgba(255, 255, 255, 0.1);
--input-bg: rgba(255, 255, 255, 0.03);
--surface-raised: #121a20;
```

5. **Create `src/lib/styles/dashboard.css`** with:
   - Custom scrollbar styling using `--scrollbar-thumb` variable (`::-webkit-scrollbar` 6px width, rounded thumb)
   - Dashboard-specific keyframe animations OUTSIDE @theme (so Tailwind 4 doesn't tree-shake them):
     - `@keyframes dash-slide-in` (opacity 0 translateX(-8px) -> opacity 1 translateX(0), 0.3s)
     - `@keyframes dash-fade-up` (opacity 0 translateY(12px) -> opacity 1 translateY(0), 0.4s)
     - `@keyframes dash-scale-in` (opacity 0 scale(0.96) -> opacity 1 scale(1), 0.3s)
     - `@keyframes wave-hand` (rotate 0deg -> 14deg -> -8deg -> 14deg -> -4deg -> 0deg, 2s infinite)
   - Utility classes: `.dash-card` (rounded-[14px], border, bg, transition, hover elevation), `.dash-section-title` (font-extrabold, tracking-tight, text-main)
   - `.mono` utility class mapping to `font-family: var(--font-mono)`

**i18n keys:**

6. **Add dashboard chrome i18n keys to `messages/en.json`** (append to end, before closing `}`):
```json
"dash_nav_overview": "Overview",
"dash_nav_calendar": "Calendar",
"dash_nav_content": "Content",
"dash_nav_campaigns": "Campaigns",
"dash_nav_publishing": "Publishing",
"dash_nav_settings": "Settings",
"dash_nav_section_main": "Main",
"dash_nav_section_tools": "AI Tools",
"dash_nav_section_system": "System",
"dash_header_search_placeholder": "Search...",
"dash_header_notifications": "Notifications",
"dash_user_name": "Diego M.",
"dash_user_plan": "Pro Plan",
"dash_meta_title_overview": "Dashboard - BriefAgent",
"dash_meta_title_calendar": "Content Calendar - BriefAgent",
"dash_meta_title_editor": "Content Editor - BriefAgent",
"dash_meta_title_brand": "Brand & Campaigns - BriefAgent",
"dash_meta_title_publishing": "Publishing Hub - BriefAgent",
"dash_breadcrumb_dashboard": "Dashboard",
"dash_breadcrumb_overview": "Overview",
"dash_breadcrumb_calendar": "Calendar",
"dash_breadcrumb_editor": "Content",
"dash_breadcrumb_brand": "Campaigns",
"dash_breadcrumb_publishing": "Publishing"
```

7. **Add corresponding Spanish translations to `messages/es.json`**:
```json
"dash_nav_overview": "Resumen",
"dash_nav_calendar": "Calendario",
"dash_nav_content": "Contenido",
"dash_nav_campaigns": "Campanas",
"dash_nav_publishing": "Publicacion",
"dash_nav_settings": "Configuracion",
"dash_nav_section_main": "Principal",
"dash_nav_section_tools": "Herramientas IA",
"dash_nav_section_system": "Sistema",
"dash_header_search_placeholder": "Buscar...",
"dash_header_notifications": "Notificaciones",
"dash_user_name": "Diego M.",
"dash_user_plan": "Plan Pro",
"dash_meta_title_overview": "Panel - BriefAgent",
"dash_meta_title_calendar": "Calendario de Contenido - BriefAgent",
"dash_meta_title_editor": "Editor de Contenido - BriefAgent",
"dash_meta_title_brand": "Marca y Campanas - BriefAgent",
"dash_meta_title_publishing": "Centro de Publicacion - BriefAgent",
"dash_breadcrumb_dashboard": "Panel",
"dash_breadcrumb_overview": "Resumen",
"dash_breadcrumb_calendar": "Calendario",
"dash_breadcrumb_editor": "Contenido",
"dash_breadcrumb_brand": "Campanas",
"dash_breadcrumb_publishing": "Publicacion"
```

Note: Use proper Spanish characters (accented n, accented o) in the actual file.
  </action>
  <verify>
    - `pnpm build` completes without errors (layout group routing works)
    - `pnpm dev` serves `/` with Navbar and Footer visible (landing page unchanged)
    - New CSS variables appear in browser DevTools on both light and dark mode
    - `npx svelte-check` passes with no type errors
  </verify>
  <done>
    - Root layout is minimal (ModeWatcher + children only)
    - Landing page at `/` renders with Navbar + Footer exactly as before
    - `src/routes/(landing)/+page.svelte` exists and serves `/`
    - `src/app.css` contains all new dashboard CSS variables in both light/dark blocks
    - `src/lib/styles/dashboard.css` exists with scrollbar styling and keyframe animations
    - `messages/en.json` and `messages/es.json` contain all `dash_*` keys (~22 each)
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard sidebar, header, and layout shell</name>
  <files>
    src/lib/components/dashboard/sidebar.svelte
    src/lib/components/dashboard/header.svelte
    src/routes/dashboard/+layout.svelte
    src/routes/dashboard/+page.svelte
  </files>
  <action>
**Sidebar component** (`src/lib/components/dashboard/sidebar.svelte`):

Build the canonical 240px sidebar based on variant-5's design. Use Svelte 5 runes throughout.

Structure:
1. **Logo area** (h-14, border-b): Small logo mark (28x28 rounded-lg with gradient --c-electric to --c-electric darker, white "B" initial) + "BriefAgent" text (Brief in --text-main, Agent in --c-electric). Links to `/`.

2. **Nav sections** with section labels ("MAIN", "AI TOOLS", "SYSTEM"):
   - MAIN section items:
     - Overview (LayoutDashboard icon) -> `/dashboard`
     - Calendar (CalendarDays icon) -> `/dashboard/calendar`
     - Content (PenTool icon) -> `/dashboard/editor`
     - Campaigns (Megaphone icon) -> `/dashboard/brand`
     - Publishing (Send icon) -> `/dashboard/publishing`
   - AI TOOLS section:
     - Analytics (BarChart3 icon) -> disabled/no link (future feature)
   - SYSTEM section:
     - Settings (Settings icon) -> disabled/no link (future feature)

3. **Active route detection**: Import `page` from `$app/stores`. For `/dashboard` exact match. For others, `pathname.startsWith(href)`. Active items: text color `--c-electric`, background `--bg-sidebar-active`, left border accent 2px `--c-electric`. Inactive: text `--text-dim`, hover text `--text-main`, hover bg `--bg-sidebar-hover`.

4. **Nav item styling**: Each item is an `<a>` with `flex items-center gap-3 px-4 py-2.5 rounded-[10px] text-[0.85rem] transition-all duration-150`. Section labels: `text-[0.65rem] font-bold uppercase tracking-[0.1em] text-[var(--text-muted)] px-4 pt-5 pb-2`.

5. **User pill** at bottom (border-t): Avatar div (32x32 rounded-lg gradient from --c-tertiary to --c-secondary, white initials), name "Diego M." (font-semibold text-[0.8rem]), role "Pro Plan" (font-mono text-[0.65rem] text --c-electric). Hover bg --bg-sidebar-hover.

6. Use i18n: `m.dash_nav_overview()`, `m.dash_nav_calendar()`, etc. for labels. `m.dash_nav_section_main()`, `m.dash_nav_section_tools()`, `m.dash_nav_section_system()` for section labels.

7. Icon import: `import { LayoutDashboard, CalendarDays, PenTool, Megaphone, Send, BarChart3, Settings } from 'lucide-svelte'`. Render via `<svelte:component this={item.icon} class="w-[18px] h-[18px]" />`.

Background: `var(--bg-sidebar)`, border-right: `1px solid var(--border-subtle)`.

**Header component** (`src/lib/components/dashboard/header.svelte`):

Based on variant-1/variant-5 header design. Accepts `title` prop for breadcrumb.

1. **Left side**: Breadcrumb showing "Dashboard / {title}" where Dashboard is dim text and title is bold text-main.
2. **Right side**: Row of icon buttons (36x36 rounded-[10px], border --border-subtle, bg --toggle-bg):
   - Search button (Search icon from lucide-svelte)
   - Notifications button (Bell icon) with orange notification dot (::after pseudo-element with --c-secondary, 7px circle)
   - ThemeToggle component (reuse existing from `$lib/components/ui/theme-toggle.svelte`, wrap in same button styling)
3. Height: h-14. Background: `var(--bg-surface)`. Border-bottom: `1px solid var(--border-subtle)`. Sticky top-0.
4. Props interface: `{ title: string }` using `$props()`.
5. Use `m.dash_header_search_placeholder()` for search aria-label, `m.dash_header_notifications()` for bell aria-label.

**Dashboard layout** (`src/routes/dashboard/+layout.svelte`):

1. Import Sidebar and DashboardHeader
2. Import `'$lib/styles/dashboard.css'`
3. Structure: CSS Grid with `grid-template-columns: 240px 1fr` and `grid-template-rows: auto 1fr`, `h-screen overflow-hidden`
4. Sidebar spans full grid-row (1 / -1)
5. Header in col 2 row 1
6. Main content area in col 2 row 2, `overflow-y-auto`, padding 2rem
7. Background: `var(--bg-page)`
8. Pass page title to header via context or derive from `$page.url.pathname`

For the breadcrumb title, derive it from the current pathname:
```typescript
import { page } from '$app/stores';
import * as m from '$lib/paraglide/messages.js';

const titleMap: Record<string, () => string> = {
  '/dashboard': () => m.dash_breadcrumb_overview(),
  '/dashboard/calendar': () => m.dash_breadcrumb_calendar(),
  '/dashboard/editor': () => m.dash_breadcrumb_editor(),
  '/dashboard/brand': () => m.dash_breadcrumb_brand(),
  '/dashboard/publishing': () => m.dash_breadcrumb_publishing(),
};
```

**Placeholder dashboard page** (`src/routes/dashboard/+page.svelte`):

Create a minimal placeholder for the Overview page that confirms the layout works:
- `<svelte:head>` with `<title>{m.dash_meta_title_overview()}</title>`
- A welcome section: `<h1>` with wave hand emoji + "Good morning, Diego" greeting
- Subtext: "Here's what's happening with your content today"
- Style the welcome using text-[1.75rem] font-extrabold, subtext in text-dim
- This will be fully built out in Plan 02

Use existing CSS variables from `app.css` throughout. Do NOT introduce any new CSS variable names — map to the ones established in Task 1.
  </action>
  <verify>
    - `pnpm dev` and navigate to `/dashboard` — see sidebar on left, header on top, placeholder content in main area
    - Click sidebar nav items — active state highlights correctly for Overview
    - Navigate to `/` — see landing page with Navbar+Footer, no sidebar
    - Theme toggle works on dashboard (sidebar, header, content all switch)
    - `npx svelte-check` passes
    - `pnpm build` succeeds
  </verify>
  <done>
    - `/dashboard` renders with 240px sidebar, 56px header, scrollable main area
    - Sidebar shows logo, 5 nav items under MAIN, 1 under AI TOOLS, 1 under SYSTEM, user pill at bottom
    - Active route is highlighted with cyan accent
    - Header shows breadcrumb and icon buttons (search, notifications, theme toggle)
    - Landing page at `/` still renders with Navbar+Footer, completely unaffected
    - Theme toggle works on dashboard pages
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes (confirms layout group routing, no import errors)
2. `pnpm dev` — visit `/` → landing page with Navbar and Footer (unchanged from before)
3. `pnpm dev` — visit `/dashboard` → dashboard shell with sidebar, header, placeholder content
4. Theme toggle works on both `/` and `/dashboard`
5. Sidebar nav items render with correct icons and i18n labels
6. `npx svelte-check` passes with no type errors
7. Browser DevTools shows new CSS variables (--bg-sidebar, --positive, --negative, etc.) in both light/dark modes
</verification>

<success_criteria>
- Layout group architecture fully working: landing at `/` with Navbar+Footer, dashboard at `/dashboard` with sidebar+header
- All 22 new i18n keys present in both en.json and es.json
- Dashboard CSS variables added to app.css (light + dark)
- dashboard.css created with animations and scrollbar styling
- Sidebar component with active route detection, lucide-svelte icons, i18n labels
- Header component with breadcrumb, search, notifications, theme toggle
- Zero regressions on landing page
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-ui/05-01-SUMMARY.md`
</output>
