---
phase: 05-dashboard-ui
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/routes/dashboard/+page.svelte
  - src/lib/components/dashboard/kpi-card.svelte
  - src/lib/components/dashboard/chart-section.svelte
  - src/lib/components/dashboard/activity-feed.svelte
  - src/lib/components/dashboard/schedule-card.svelte
  - src/lib/components/dashboard/stat-card.svelte
  - src/lib/data/mock-overview.ts
  - messages/en.json
  - messages/es.json
autonomous: true

must_haves:
  truths:
    - "User sees 4 KPI cards with values, trends, and sparkline charts at the top of the Overview page"
    - "User sees a performance chart section with tab controls"
    - "User sees an activity feed with timestamped entries"
    - "User sees an upcoming schedule section with scheduled content cards"
    - "All data is static mock data that can be replaced with real API calls later"
  artifacts:
    - path: "src/routes/dashboard/+page.svelte"
      provides: "Overview page composing KPI cards, chart, activity feed, schedule"
      min_lines: 40
    - path: "src/lib/components/dashboard/kpi-card.svelte"
      provides: "KPI metric card with trend badge and SVG sparkline"
      min_lines: 40
    - path: "src/lib/components/dashboard/chart-section.svelte"
      provides: "Chart container with period tabs and SVG area chart"
      min_lines: 50
    - path: "src/lib/components/dashboard/activity-feed.svelte"
      provides: "Timeline of recent activities with icons and timestamps"
      min_lines: 30
    - path: "src/lib/components/dashboard/schedule-card.svelte"
      provides: "Upcoming scheduled content card with platform badge and time"
      min_lines: 25
    - path: "src/lib/data/mock-overview.ts"
      provides: "Typed mock data for all Overview page components"
      exports: ["kpiCards", "chartData", "activityItems", "scheduleItems"]
  key_links:
    - from: "src/routes/dashboard/+page.svelte"
      to: "src/lib/data/mock-overview.ts"
      via: "import mock data"
      pattern: "import.*mock-overview"
    - from: "src/routes/dashboard/+page.svelte"
      to: "src/lib/components/dashboard/kpi-card.svelte"
      via: "import KpiCard"
      pattern: "import KpiCard"
    - from: "src/lib/components/dashboard/kpi-card.svelte"
      to: "mock-overview.ts types"
      via: "KpiCard interface props"
      pattern: "interface Props"
---

<objective>
Build the Overview / Analytics dashboard page (variant-1) with KPI cards, performance chart, activity feed, and schedule.

Purpose: This is the dashboard's landing page and the first view users see. It establishes the component patterns (stat cards, chart containers, feed lists) that other views will reference. Uses mock data throughout.

Output: A polished Overview page at `/dashboard` matching variant-1's design, composed of reusable dashboard components.
</objective>

<execution_context>
@/home/diego/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-ui/05-RESEARCH.md
@.planning/phases/05-dashboard-ui/05-01-SUMMARY.md

@design-variants/dashboard/variant-1.html
@src/app.css
@src/lib/styles/theme.css
@src/lib/styles/dashboard.css
@messages/en.json
@messages/es.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mock data + KPI card and stat card components</name>
  <files>
    src/lib/data/mock-overview.ts
    src/lib/components/dashboard/kpi-card.svelte
    src/lib/components/dashboard/stat-card.svelte
    messages/en.json
    messages/es.json
  </files>
  <action>
**Mock data file** (`src/lib/data/mock-overview.ts`):

Create fully typed mock data matching what variant-1 displays. Export interfaces AND data arrays.

```typescript
// Types
export interface KpiCard {
  id: string;
  labelKey: string;  // i18n key name (e.g., 'dash_kpi_total_posts')
  value: string;
  unit?: string;
  trend: { direction: 'up' | 'down'; value: string };
  sparkline: number[];
  color: 'electric' | 'secondary' | 'tertiary' | 'positive';
}

export interface ChartDataPoint {
  label: string;
  impressions: number;
  engagement: number;
}

export interface ActivityItem {
  id: string;
  type: 'published' | 'scheduled' | 'generated' | 'approved' | 'edited';
  title: string;
  time: string;
  platform?: string;
}

export interface ScheduleItem {
  id: string;
  title: string;
  platform: string;
  time: string;
  type: 'carousel' | 'image' | 'text' | 'video';
  status: 'scheduled' | 'review';
}

export interface QuickStat {
  labelKey: string;
  value: string;
  sublabel: string;
}
```

KPI data (4 cards matching variant-1):
1. Total Posts: value "248", trend up 12%, sparkline [20,25,18,30,28,35,32,40], color "electric"
2. Engagement Rate: value "4.8", unit "%", trend up 0.6%, sparkline [3.2,3.8,4.1,3.9,4.3,4.6,4.2,4.8], color "secondary"
3. Impressions: value "12.4", unit "K", trend down 3%, sparkline [15,14,13,12,13,11,12,12.4], color "tertiary"
4. Click-through: value "2.1", unit "%", trend up 0.3%, sparkline [1.4,1.6,1.8,1.7,1.9,2.0,1.8,2.1], color "positive"

Chart data (7-day series):
```typescript
export const chartData: ChartDataPoint[] = [
  { label: 'Mon', impressions: 2400, engagement: 180 },
  { label: 'Tue', impressions: 3200, engagement: 240 },
  { label: 'Wed', impressions: 2800, engagement: 210 },
  { label: 'Thu', impressions: 4100, engagement: 310 },
  { label: 'Fri', impressions: 3600, engagement: 280 },
  { label: 'Sat', impressions: 2100, engagement: 160 },
  { label: 'Sun', impressions: 2900, engagement: 220 },
];
```

Activity items (6 entries):
- "LinkedIn carousel published" (published, 2h ago)
- "Instagram reel scheduled" (scheduled, 4h ago)
- "3 new post variants generated" (generated, 6h ago)
- "Product launch campaign approved" (approved, 8h ago)
- "Blog post copy edited" (edited, 12h ago)
- "Weekly content plan generated" (generated, 1d ago)

Schedule items (4 entries):
- "5 AI Tools Every Startup Needs" (LinkedIn, carousel, 2:00 PM, scheduled)
- "Behind the Scenes: Our Design Process" (Instagram, image, 4:30 PM, scheduled)
- "Weekly Growth Tips Thread" (Twitter/X, text, Tomorrow 9:00 AM, review)
- "Product Demo Walkthrough" (LinkedIn, video, Tomorrow 11:00 AM, scheduled)

Quick stats (3 items):
- Posts This Week: "12", "3 remaining"
- Best Day: "Thursday", "4.2K impressions"
- Top Platform: "LinkedIn", "68% of traffic"

**KPI Card component** (`src/lib/components/dashboard/kpi-card.svelte`):

Props: `{ label: string; value: string; unit?: string; trend: { direction: 'up' | 'down'; value: string }; sparkline: number[]; accentColor: string }`

Build matching variant-1's card design:
- Outer container: rounded-[14px], border --border-subtle, bg --bg-surface, p-5, transition hover:-translate-y-0.5, hover:border-[var(--border-hover)]
- Top accent line on hover: absolute top-0 left-0 right-0 h-0.5, opacity-0 group-hover:opacity-100
- Label: text-xs --text-dim
- Value: text-[1.85rem] font-extrabold tracking-tight. Unit suffix in text-sm font-normal --text-dim
- Bottom row: trend badge (left) + SVG sparkline (right)
- Trend badge: inline-flex items-center gap-1, text-[0.7rem] font-bold, rounded-md px-2 py-0.5. Up = --positive bg with 10% opacity. Down = --negative bg with 10% opacity. Triangle character before value.
- SVG sparkline: 72x28 viewBox. Compute polyline points from sparkline data using `$derived`. Area fill at 8% opacity, stroke at 1.5px width, stroke-linecap round.
- CSS animation: entry animation using `dash-fade-up` from dashboard.css with staggered `animation-delay` passed as prop or CSS variable.

Map accentColor from the color prop:
- 'electric' -> 'var(--c-electric)'
- 'secondary' -> 'var(--c-secondary)'
- 'tertiary' -> 'var(--c-tertiary)'
- 'positive' -> 'var(--positive)'

**Stat card component** (`src/lib/components/dashboard/stat-card.svelte`):

Smaller card for quick stats. Props: `{ label: string; value: string; sublabel: string }`.
- rounded-xl, border --border-subtle, bg --bg-surface, p-4
- Label: text-xs --text-dim, mb-1
- Value: text-lg font-extrabold --text-main
- Sublabel: text-xs --text-dim, mt-0.5

**i18n keys to add** (append to en.json and es.json):

en.json:
```json
"dash_kpi_total_posts": "Total Posts",
"dash_kpi_engagement_rate": "Engagement Rate",
"dash_kpi_impressions": "Impressions",
"dash_kpi_click_through": "Click-through",
"dash_overview_welcome": "Good morning, Diego",
"dash_overview_subtitle": "Here's what's happening with your content today.",
"dash_overview_performance": "Performance",
"dash_overview_period_7d": "7 days",
"dash_overview_period_30d": "30 days",
"dash_overview_period_90d": "90 days",
"dash_overview_impressions_label": "Impressions",
"dash_overview_engagement_label": "Engagement",
"dash_overview_activity": "Recent Activity",
"dash_overview_view_all": "View all",
"dash_overview_schedule": "Upcoming Schedule",
"dash_overview_quick_stats": "Quick Stats",
"dash_overview_stat_posts_week": "Posts This Week",
"dash_overview_stat_best_day": "Best Day",
"dash_overview_stat_top_platform": "Top Platform",
"dash_activity_published": "Published",
"dash_activity_scheduled": "Scheduled",
"dash_activity_generated": "Generated",
"dash_activity_approved": "Approved",
"dash_activity_edited": "Edited"
```

es.json (Spanish equivalents):
```json
"dash_kpi_total_posts": "Publicaciones Totales",
"dash_kpi_engagement_rate": "Tasa de Interaccion",
"dash_kpi_impressions": "Impresiones",
"dash_kpi_click_through": "Tasa de Clics",
"dash_overview_welcome": "Buenos dias, Diego",
"dash_overview_subtitle": "Esto es lo que esta pasando con tu contenido hoy.",
"dash_overview_performance": "Rendimiento",
"dash_overview_period_7d": "7 dias",
"dash_overview_period_30d": "30 dias",
"dash_overview_period_90d": "90 dias",
"dash_overview_impressions_label": "Impresiones",
"dash_overview_engagement_label": "Interaccion",
"dash_overview_activity": "Actividad Reciente",
"dash_overview_view_all": "Ver todo",
"dash_overview_schedule": "Proximos Programados",
"dash_overview_quick_stats": "Estadisticas Rapidas",
"dash_overview_stat_posts_week": "Publicaciones Esta Semana",
"dash_overview_stat_best_day": "Mejor Dia",
"dash_overview_stat_top_platform": "Plataforma Principal",
"dash_activity_published": "Publicado",
"dash_activity_scheduled": "Programado",
"dash_activity_generated": "Generado",
"dash_activity_approved": "Aprobado",
"dash_activity_edited": "Editado"
```

Note: Use proper Spanish accented characters in the actual file.
  </action>
  <verify>
    - `npx svelte-check` passes (TypeScript types valid)
    - Mock data file exports all 5 arrays/objects without type errors
    - KPI card renders sparkline SVG correctly (check in browser at `/dashboard`)
  </verify>
  <done>
    - `mock-overview.ts` exports typed interfaces and data for KPIs, chart, activity, schedule, quick stats
    - `kpi-card.svelte` renders card with value, trend badge, and SVG sparkline
    - `stat-card.svelte` renders simple metric card
    - ~23 new i18n keys in both en.json and es.json
  </done>
</task>

<task type="auto">
  <name>Task 2: Chart section, activity feed, schedule, and Overview page composition</name>
  <files>
    src/lib/components/dashboard/chart-section.svelte
    src/lib/components/dashboard/activity-feed.svelte
    src/lib/components/dashboard/schedule-card.svelte
    src/routes/dashboard/+page.svelte
  </files>
  <action>
**Chart section component** (`src/lib/components/dashboard/chart-section.svelte`):

Props: `{ data: ChartDataPoint[]; title: string }` (import ChartDataPoint from mock-overview).

Design (matching variant-1):
- Card container: rounded-[14px], border --border-subtle, bg --bg-surface, overflow hidden
- Header row: title (font-bold text-[0.95rem] --text-main) + period tabs on right
- Period tabs: 3 buttons ("7 days", "30 days", "90 days") using i18n keys. Active tab: bg --bg-sidebar-active, text --c-electric, font-semibold. Inactive: text --text-dim, hover text --text-main. Rounded-lg, px-3 py-1.5, text-xs. Use `$state` for selected tab, but all show same data (mock).
- Chart area: SVG with viewBox "0 0 600 200". Two area charts overlaid:
  - Impressions line: color --c-electric, area fill 8% opacity, stroke 2px
  - Engagement line: color --c-secondary, area fill 8% opacity, stroke 2px
- X-axis labels: day names from data, positioned along bottom, text-[0.65rem] --text-muted
- Grid lines: 4 horizontal dashed lines, stroke --border-subtle, opacity 0.5
- Legend: Two items (Impressions dot + label, Engagement dot + label) below chart, text-xs --text-dim
- Compute SVG path points using `$derived` from data array. Scale impressions to chart height (0-200). Scale engagement to same range for visibility.

**Activity feed component** (`src/lib/components/dashboard/activity-feed.svelte`):

Props: `{ items: ActivityItem[] }` (import type from mock-overview).

Design (matching variant-1):
- Card container: rounded-[14px], border, bg --bg-surface
- Header: "Recent Activity" title + "View all" link (text --c-electric, text-xs)
- List of items, each with:
  - Left: colored dot (8px circle) based on type: published=--positive, scheduled=--c-electric, generated=--c-secondary, approved=--positive, edited=--text-dim
  - Center: title (text-sm --text-main) + type badge (text-[0.65rem] px-2 py-0.5 rounded-md, color matching dot, bg at 10% opacity)
  - Right: time string (text-xs --text-muted, font-mono)
- Items separated by 1px border-b --border-subtle (not on last)
- Padding: p-4 for header, px-4 py-3 for each item

**Schedule card component** (`src/lib/components/dashboard/schedule-card.svelte`):

Props: `{ items: ScheduleItem[] }` (import type from mock-overview).

Design:
- Card container: rounded-[14px], border, bg --bg-surface
- Header: "Upcoming Schedule" title
- Each item: flex row with:
  - Left: platform icon indicator (colored bar 3px wide, 100% height, color by platform: LinkedIn=--c-electric, Instagram=--c-tertiary, Twitter=--c-secondary)
  - Center: title (text-sm font-medium --text-main) + platform name + content type badge
  - Right: time (text-xs font-mono --text-muted) + status badge (scheduled=--c-electric bg, review=--c-secondary bg)
- Items in a vertical list with gap-2, px-4 py-3 each

**Overview page** (`src/routes/dashboard/+page.svelte`):

Replace the placeholder from Plan 01. Compose all components:

```
<svelte:head><title>{m.dash_meta_title_overview()}</title></svelte:head>

1. Welcome section (mb-6):
   - h1: wave emoji + "Good morning, Diego" (text-[1.75rem] font-extrabold tracking-tight)
   - p: subtitle (text-sm --text-dim). "Here's what's happening..." with "today" in --c-electric font-bold

2. KPI cards row (grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-4, mb-6):
   - 4 KpiCard components from kpiCards mock data
   - Map color to accentColor CSS variable
   - Stagger entry animation: each card gets animation-delay (0, 0.05s, 0.1s, 0.15s)

3. Two-column layout (grid grid-cols-1 lg:grid-cols-3 gap-4, mb-6):
   - Left (col-span-2): ChartSection with chartData
   - Right (col-span-1): Column with 3 QuickStat cards stacked

4. Two-column layout (grid grid-cols-1 lg:grid-cols-2 gap-4):
   - Left: ActivityFeed with activityItems
   - Right: ScheduleCard with scheduleItems
```

Import all components and mock data. Use i18n for all labels. Keep the page file under 80 lines by delegating to child components.

Apply `dash-fade-up` animation to each section with staggered delays for a polished page-load feel.
  </action>
  <verify>
    - `pnpm dev` — visit `/dashboard` and see all 4 sections: KPI cards, chart, activity feed, schedule
    - KPI sparklines render as SVG curves (not flat lines)
    - Chart shows two overlaid area charts with x-axis labels
    - Activity feed shows 6 items with colored dots and time
    - Schedule shows 4 upcoming items with platform indicators
    - Theme toggle switches all components correctly
    - `pnpm build` succeeds
    - `npx svelte-check` passes
  </verify>
  <done>
    - Overview page at `/dashboard` renders all 4 sections matching variant-1 design
    - KPI cards show values, trends, and sparklines
    - Performance chart renders dual-series area chart with period tabs
    - Activity feed displays typed, timestamped entries
    - Schedule section shows upcoming content with platform and status
    - Quick stats cards display in sidebar column
    - Page composes ~6 child components, stays under 80 lines
    - All visible text uses i18n (except mock data content strings)
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. `/dashboard` renders a complete Overview page with all sections
3. KPI cards (4), performance chart, activity feed (6 items), schedule (4 items), quick stats (3) all visible
4. SVG sparklines and area chart render correctly with proper colors
5. Theme toggle switches all dashboard components (light/dark)
6. Page under 80 lines, each component under 200 lines
7. `npx svelte-check` passes
8. Mock data is cleanly separated in `src/lib/data/mock-overview.ts` with exported types
</verification>

<success_criteria>
- Complete Overview / Analytics page matching variant-1 at `/dashboard`
- 6 new components: kpi-card, chart-section, activity-feed, schedule-card, stat-card + page
- Typed mock data file with exported interfaces
- All visible text i18n'd (~23 new keys in en/es)
- Staggered entry animations on page load
- Responsive grid layout (1 col mobile, 2 col tablet, full desktop)
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-ui/05-02-SUMMARY.md`
</output>
