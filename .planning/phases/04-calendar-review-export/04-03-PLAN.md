---
phase: 04-calendar-review-export
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/routes/dashboard/+page.server.ts
  - src/routes/dashboard/+page.svelte
  - messages/en.json
  - messages/es.json
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "Dashboard shows upcoming posts for the next 7 days from real database data"
    - "Dashboard shows quick stats: total posts generated, approved count, pending review count"
    - "Dashboard has a prominent button to generate a new content plan that links to /dashboard/generate"
  artifacts:
    - path: "src/routes/dashboard/+page.server.ts"
      provides: "Server load for dashboard stats and upcoming posts"
      exports: ["load"]
    - path: "src/routes/dashboard/+page.svelte"
      provides: "Dashboard overview wired to real data with generate plan CTA"
  key_links:
    - from: "src/routes/dashboard/+page.server.ts"
      to: "src/lib/server/db/schema.ts"
      via: "Drizzle aggregate count + upcoming posts query"
      pattern: "db\\.query\\.posts|count\\(\\)"
    - from: "src/routes/dashboard/+page.svelte"
      to: "/dashboard/generate"
      via: "CTA button linking to generate page"
      pattern: "href.*dashboard/generate"
---

<objective>
Wire the dashboard overview page to display real content statistics and upcoming posts from the database, replacing all mock data with server-loaded data, and add a generate plan CTA button.

Purpose: Transforms the dashboard from a static mockup into a live content management overview, giving users an at-a-glance view of their content pipeline status.
Output: Dashboard overview showing real stats, upcoming posts, and quick access to content plan generation.
</objective>

<execution_context>
@/home/diego/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calendar-review-export/04-RESEARCH.md
@.planning/phases/04-calendar-review-export/04-01-SUMMARY.md
@.planning/phases/05-dashboard-ui/05-02-SUMMARY.md
@src/routes/dashboard/+page.svelte
@src/routes/dashboard/+layout.server.ts
@src/lib/data/mock-overview.ts
@src/lib/components/dashboard/kpi-card.svelte
@src/lib/components/dashboard/schedule-card.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard server load with stats and upcoming posts</name>
  <files>
    src/routes/dashboard/+page.server.ts
  </files>
  <action>
    **Create src/routes/dashboard/+page.server.ts:**

    - Import `db`, `posts` from schema, `eq`, `and`, `gte`, `lte`, `count`, `sql` from drizzle-orm
    - Import `addDays`, `startOfDay` from date-fns
    - Import type `PageServerLoad` from `./$types`

    **`load` function:**
    1. Get `product` from `await parent()` — return `{ stats: null, upcoming: [], hasProduct: false }` if no product
    2. **Stats query** — aggregate counts by status:
       ```
       db.select({ status: posts.status, count: count() })
         .from(posts)
         .where(eq(posts.productId, product.id))
         .groupBy(posts.status)
       ```
       Compute:
       - `generated`: sum of all counts (total posts ever generated)
       - `approved`: count where status === 'approved'
       - `pendingReview`: count where status === 'pending_review'
       - `published`: count where status === 'published'
       - `rejected`: count where status === 'rejected'

    3. **Upcoming posts query** — next 7 days:
       ```
       db.query.posts.findMany({
         where: and(
           eq(posts.productId, product.id),
           gte(posts.scheduledAt, startOfDay(now)),
           lte(posts.scheduledAt, addDays(now, 7))
         ),
         columns: { id, topic, status, scheduledAt, platform, postType },
         orderBy: posts.scheduledAt,
         limit: 10
       })
       ```

    4. Return `{ stats, upcoming: serialized (scheduledAt as ISO string), hasProduct: true }`
  </action>
  <verify>
    - `npx svelte-check` passes for the new file
    - File exports a `load` function typed as PageServerLoad
    - Stats query groups by status and computes totals
    - Upcoming query filters to next 7 days with limit 10
  </verify>
  <done>
    Dashboard server load provides real aggregate stats (total/approved/pending/published counts) and upcoming posts for the next 7 days from the database.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard overview page to real data with generate plan CTA</name>
  <files>
    src/routes/dashboard/+page.svelte
    messages/en.json
    messages/es.json
  </files>
  <action>
    **1. Restructure dashboard page (src/routes/dashboard/+page.svelte):**

    - Remove ALL mock data imports: `kpiCards`, `chartData`, `activityItems`, `scheduleItems`, `quickStats` from `$lib/data/mock-overview.js`
    - Accept `data` prop: `let { data } = $props()` — this provides `data.stats`, `data.upcoming`, `data.hasProduct`

    **2. Replace KPI cards section:**
    - Instead of the 4 mock KPI cards (total posts, engagement rate, impressions, click-through), display 4 real stat cards:
      - "Posts Generated" — `data.stats?.generated ?? 0` — color: electric
      - "Pending Review" — `data.stats?.pendingReview ?? 0` — color: secondary (orange)
      - "Approved" — `data.stats?.approved ?? 0` — color: electric (cyan)
      - "Published" — `data.stats?.published ?? 0` — color: positive (green)
    - Use the existing `KpiCard` component but without sparklines (pass empty array). Or use `StatCard` for simpler display. Choose whichever looks cleaner — `StatCard` is simpler and more appropriate for counts.
    - If `data.stats` is null (no product), show an empty state message prompting user to complete onboarding.

    **3. Replace chart section + quick stats:**
    - Remove the ChartSection and quickStats entirely (they were for engagement analytics which don't exist yet in the MVP — no real engagement data to chart)
    - Replace with a prominent "Generate Content Plan" CTA section:
      - If `data.hasProduct`: Show a card with heading "Ready to create content?", description "Generate a 2-week content plan powered by AI", and a large button linking to `/dashboard/generate` with a Wand2 or Sparkles icon. Style the card with a gradient border or subtle glow using `var(--c-electric)`.
      - If no product: Show a card prompting to complete onboarding with link to `/dashboard/onboarding`

    **4. Replace Activity Feed:**
    - Remove the ActivityFeed component (mock data, no real activity tracking in MVP)
    - This space is now taken by the CTA section above

    **5. Replace Schedule section with real upcoming posts:**
    - Keep the ScheduleCard component pattern but feed it real data from `data.upcoming`
    - Map upcoming posts to the ScheduleCard expected format: `{ title: post.topic, platform: post.platform, time: formatted scheduledAt, status: post.status }`
    - Use `date-fns format` for readable date/time (e.g., "Feb 19, 10:00 AM")
    - Import `postStatusColors` from `$lib/utils/post-status.ts` for status badges
    - If no upcoming posts, show empty state: "No upcoming posts. Generate a content plan to get started."
    - Each upcoming post row should be clickable, linking to the calendar page for that month

    **6. Add i18n keys (messages/en.json + messages/es.json):**
    - `dash_stat_generated`: "Posts Generated" / "Posts Generados"
    - `dash_stat_pending`: "Pending Review" / "Revision Pendiente"
    - `dash_stat_approved`: "Approved" / "Aprobados"
    - `dash_stat_published`: "Published" / "Publicados"
    - `dash_cta_title`: "Ready to create content?" / "Listo para crear contenido?"
    - `dash_cta_description`: "Generate a 2-week content plan powered by AI" / "Genera un plan de contenido de 2 semanas con IA"
    - `dash_cta_button`: "Generate Content Plan" / "Generar Plan de Contenido"
    - `dash_cta_onboarding`: "Complete your product setup to start generating" / "Completa la configuracion de tu producto para empezar"
    - `dash_cta_onboarding_btn`: "Start Setup" / "Iniciar Configuracion"
    - `dash_upcoming_title`: "Upcoming Posts" / "Proximas Publicaciones"
    - `dash_upcoming_empty`: "No upcoming posts. Generate a content plan to get started." / "Sin publicaciones proximas. Genera un plan de contenido para empezar."
    - `dash_upcoming_next_7`: "Next 7 Days" / "Proximos 7 Dias"
  </action>
  <verify>
    - `npx svelte-check` passes for dashboard page
    - `pnpm build` succeeds
    - Dashboard page no longer imports from `$lib/data/mock-overview.js`
    - Dashboard shows 4 real stat cards from server data
    - Dashboard has "Generate Content Plan" CTA button linking to /dashboard/generate
    - Dashboard shows upcoming posts from server data with status badges
    - Empty states render correctly when no product or no posts
  </verify>
  <done>
    Dashboard overview page wired to real database data showing content pipeline stats (generated/pending/approved/published counts), upcoming posts for next 7 days with status badges, and a prominent CTA to generate new content plans. All mock data imports removed.
  </done>
</task>

</tasks>

<verification>
- Dashboard server load returns aggregate stats and upcoming posts from real DB data
- Dashboard page displays 4 stat cards with real counts (not mock engagement metrics)
- Dashboard has prominent "Generate Content Plan" button linking to /dashboard/generate
- Upcoming posts section shows next 7 days of scheduled content with status colors
- Empty states handled gracefully (no product, no posts)
- No remaining imports from mock-overview.ts in dashboard route
- All files pass svelte-check and build
</verification>

<success_criteria>
- Dashboard shows real content statistics (posts generated, pending review, approved, published)
- Dashboard shows upcoming posts for next 7 days from database
- Dashboard has button to generate a new content plan
- All mock data imports removed from dashboard overview page
</success_criteria>

<output>
After completion, create `.planning/phases/04-calendar-review-export/04-03-SUMMARY.md`
</output>
