---
phase: 01-landing-page-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/components/landing/navbar.svelte
  - src/lib/components/landing/hero.svelte
  - src/lib/components/landing/features.svelte
  - src/lib/components/landing/how-it-works.svelte
  - src/lib/components/landing/pricing.svelte
  - src/lib/components/landing/cta.svelte
  - src/lib/components/landing/footer.svelte
  - src/routes/+layout.svelte
  - src/routes/+page.svelte
  - messages/en.json
  - messages/es.json
autonomous: false

must_haves:
  truths:
    - "A visitor landing on the page sees a hero section with tagline, product explanation, and CTA button"
    - "Scrolling down reveals a features section with 6 feature cards that animate in with staggered delays"
    - "A how-it-works section shows 3 steps (Brief, Generate, Publish) with connecting visual elements"
    - "A pricing section shows Free, Pro, and Agency tiers with beta badge and 'free during beta' messaging"
    - "A final CTA section invites the visitor to sign up"
    - "The navbar is sticky with backdrop blur and contains logo, nav links, language switcher, theme toggle, and CTA"
    - "All text content switches between English and Spanish when the language is changed"
    - "All sections support dark mode with appropriate color adjustments"
    - "Hover micro-interactions are present on buttons (scale/glow), cards (lift/tilt), and nav links (color transition)"
    - "Scroll-triggered reveal animations fire as each section enters the viewport"
  artifacts:
    - path: "src/lib/components/landing/navbar.svelte"
      provides: "Sticky navbar with blur, logo, nav links, language switcher, theme toggle, CTA"
    - path: "src/lib/components/landing/hero.svelte"
      provides: "Hero section with gradient background, tagline, description, CTA buttons, product mockup"
    - path: "src/lib/components/landing/features.svelte"
      provides: "6 feature cards in 3x2 grid with icons, titles, descriptions, scroll reveals"
    - path: "src/lib/components/landing/how-it-works.svelte"
      provides: "3-step flow with numbered steps, connecting lines, icons, descriptions"
    - path: "src/lib/components/landing/pricing.svelte"
      provides: "3 pricing tiers (Free, Pro, Agency) with beta badge, feature lists, CTA buttons"
    - path: "src/lib/components/landing/cta.svelte"
      provides: "Final call-to-action section with gradient background and signup button"
    - path: "src/lib/components/landing/footer.svelte"
      provides: "Footer with logo, tagline, nav links, social links, copyright"
    - path: "src/routes/+page.svelte"
      provides: "Landing page composing all sections in order"
    - path: "src/routes/+layout.svelte"
      provides: "Root layout with navbar and footer wrapping page content"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/landing/*.svelte"
      via: "imports and renders each section component"
      pattern: "import.*landing/(hero|features|how-it-works|pricing|cta)"
    - from: "src/lib/components/landing/hero.svelte"
      to: "src/lib/components/ui/button.svelte"
      via: "uses Button component for CTA"
      pattern: "import.*Button|<Button"
    - from: "src/lib/components/landing/pricing.svelte"
      to: "src/lib/components/ui/card.svelte"
      via: "uses Card component for pricing tiers"
      pattern: "import.*Card|<Card"
    - from: "src/lib/components/landing/features.svelte"
      to: "src/lib/utils/animations.ts"
      via: "uses scroll reveal animation config"
      pattern: "import.*animations|inview"
    - from: "src/lib/components/landing/navbar.svelte"
      to: "src/lib/components/ui/theme-toggle.svelte"
      via: "renders ThemeToggle in navbar"
      pattern: "import.*ThemeToggle|<ThemeToggle"
    - from: "src/lib/components/landing/navbar.svelte"
      to: "src/lib/components/ui/language-switcher.svelte"
      via: "renders LanguageSwitcher in navbar"
      pattern: "import.*LanguageSwitcher|<LanguageSwitcher"
---

<objective>
Build the complete landing page by composing section components (Navbar, Hero, Features, How It Works, Pricing, CTA, Footer) using the design system from Plan 01-01. Every section has rich scroll-triggered animations, hover micro-interactions, dark mode support, and full EN/ES i18n.

Purpose: Deliver the public-facing landing page that communicates BriefAgent's value proposition to visitors. This is the first thing users see -- it must be vibrant, creative, and polished per the locked design direction (Gumloop/Relume.io aesthetic).

Output: A fully functional, animated, bilingual landing page at the root route.
</objective>

<execution_context>
@/home/diego/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-page-design-system/01-RESEARCH.md
@.planning/phases/01-landing-page-design-system/01-CONTEXT.md
@.planning/phases/01-landing-page-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Navbar, Hero, and Features sections</name>
  <files>
    src/lib/components/landing/navbar.svelte
    src/lib/components/landing/hero.svelte
    src/lib/components/landing/features.svelte
    src/routes/+layout.svelte
    src/routes/+page.svelte
    messages/en.json
    messages/es.json
  </files>
  <action>
    Build the top three sections of the landing page. Use the design system components from Plan 01-01 (Button, Card, Badge, Section, Container, ThemeToggle, LanguageSwitcher). Import Paraglide messages with `m` from `$lib/paraglide/messages.js`. Use svelte-inview for scroll-triggered reveals. All Lucide icons imported from `lucide-svelte`.

    **IMPORTANT: Use the frontend-design skill for all UI work.** The design must be vibrant, creative, and match the Gumloop/Relume.io aesthetic -- not generic SaaS. Bold gradients, strong colors, polished interactions.

    **Navbar** (`src/lib/components/landing/navbar.svelte`):
    - Fixed position, full width, z-50
    - Transparent on load, transitions to glass-morphism on scroll (after 20px): `bg-white/80 dark:bg-neutral-950/80 backdrop-blur-xl shadow-md`
    - Use a `$state` variable for scroll position, tracked via `$effect` with scroll event listener (passive)
    - Left: Logo text "BriefAgent" with `font-display font-bold text-xl` -- link to `localizeHref('/')`
    - Center (hidden on mobile, `hidden md:flex`): nav links (Features, How it works, Pricing) as anchor links (`#features`, `#how-it-works`, `#pricing`), styled with `text-sm font-medium text-neutral-600 dark:text-neutral-400 hover:text-neutral-900 dark:hover:text-neutral-100 transition-colors`
    - Right: LanguageSwitcher + ThemeToggle + CTA Button (primary, sm, href="/signup")
    - Mobile: Include a hamburger menu button (visible `md:hidden`) that toggles a mobile nav drawer/dropdown. Use Bits UI Sheet or a custom slide-in panel. Mobile nav should show all nav links, language switcher, theme toggle, and CTA.
    - All text via Paraglide: `m.nav_features()`, `m.nav_how_it_works()`, `m.nav_pricing()`, `m.nav_get_started()`

    **Hero** (`src/lib/components/landing/hero.svelte`):
    - Use Section component with `variant="gradient"` (dark gradient background: primary-950 -> neutral-950 -> primary-900)
    - Large padding top to account for fixed navbar (add `pt-32 md:pt-40` in addition to section padding)
    - Layout: Two columns on desktop (`grid grid-cols-1 lg:grid-cols-2 gap-12 items-center`), stacked on mobile
    - Left column:
      - Small Badge with accent variant: "AI-Powered Marketing" or similar (via i18n key)
      - Tagline: `m.landing_hero_tagline()` -- use `text-4xl md:text-5xl lg:text-6xl font-display font-extrabold leading-tight tracking-tight text-white`
      - Description: `m.landing_hero_description()` -- use `text-lg md:text-xl text-neutral-300 leading-relaxed mt-6`
      - Two CTA buttons: Primary large (`m.landing_hero_cta()`, href="/signup") + Ghost/outline (`m.landing_hero_cta_secondary()`, href="#features")
      - Entry animations: CSS animation classes (NOT Svelte `in:` transitions -- hero is above the fold, avoid SSR hydration flicker). Use `animate-fade-in-up` with staggered `animation-delay` on tagline, description, and buttons.
    - Right column:
      - A product mockup placeholder. Create a styled div that looks like a dashboard/calendar screenshot placeholder. Use `rounded-2xl overflow-hidden shadow-glow-lg` with a gradient border. Inside, show a schematic layout with colored blocks representing calendar entries, using the primary/accent/neutral palette. Add `animate-float` for a subtle floating effect.
      - This is a visual placeholder -- not a real screenshot. Make it look polished with the design tokens.
    - Subtle background effects: Add 2-3 decorative gradient blobs (`absolute` positioned, blurred circles with primary/accent colors, `opacity-20`, `animate-float` with different durations). These create visual depth without being distracting.

    **Features** (`src/lib/components/landing/features.svelte`):
    - Use Section component with `variant="light"` and `id="features"`
    - Section header: Title (`m.landing_features_title()`) + subtitle (`m.landing_features_subtitle()`) centered, with `font-display font-bold text-3xl md:text-4xl` and `text-neutral-600 dark:text-neutral-400`
    - 6 features in a `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8` layout
    - Each feature card uses the Card component with:
      - Lucide icon at the top (choose appropriate icons: Lightbulb, Palette, PenTool, Send, Brain, ShieldCheck or similar)
      - Icon in a colored circle: `w-12 h-12 rounded-xl bg-primary-100 dark:bg-primary-900/50 flex items-center justify-center` with icon in `text-primary-500`
      - Feature title: `font-display font-semibold text-lg mt-4`
      - Feature description: `text-neutral-600 dark:text-neutral-400 text-sm mt-2 leading-relaxed`
    - Scroll-triggered reveal: Use `svelte-inview` on the grid container. When visible, each card fades in and slides up with staggered delay (150ms * index). Use CSS transitions: `transition-all duration-700 ease-out` with `opacity-0 translate-y-8` -> `opacity-100 translate-y-0`, controlled by `style="transition-delay: {i * 150}ms"`
    - The 6 features (per research discretion recommendations):
      1. AI Content Strategy -- full 2-week plans from your brief
      2. Original AI Visuals -- brand-informed image generation
      3. Platform-Optimized Copy -- hooks, body, CTAs tailored per platform
      4. One-Click Publishing -- schedule and auto-publish to LinkedIn
      5. Deep Brand Understanding -- AI learns your voice and audience
      6. Review and Approve -- AI proposes, you dispose
    - All feature text via Paraglide messages

    **Update root layout** (`src/routes/+layout.svelte`):
    - Import and render Navbar at the top (outside `{@render children()}`)
    - Keep ModeWatcher, font imports, and app.css import from Plan 01-01

    **Update page** (`src/routes/+page.svelte`):
    - Replace the Plan 01-01 demo page
    - Import and render Hero and Features components in order
    - This will be completed in Task 2 with remaining sections

    **Update i18n if needed**: If any new message keys are needed beyond what Plan 01-01 created (hero badge text, secondary CTA, etc.), add them to both `messages/en.json` and `messages/es.json`.

    **Design quality bar:** This is a creative agency platform landing page. The hero MUST feel dramatic and premium (dark gradient, bold typography, glowing product mockup). The features section MUST feel clean and organized (light background, well-spaced cards with subtle hover effects). Study the Gumloop.com and Relume.io aesthetics and match that level of visual polish.
  </action>
  <verify>
    1. `npm run build` completes without errors
    2. `npm run dev` shows the landing page with Navbar, Hero, and Features sections
    3. Navbar is transparent at top, becomes blurred glass on scroll
    4. Hero shows gradient background, tagline, description, two CTA buttons, and product mockup placeholder
    5. Hero elements have CSS fade-in-up animations on page load (no SSR hydration flash)
    6. Features shows 6 cards in a 3x2 grid (3 cols on lg, 2 on md, 1 on mobile)
    7. Feature cards animate in with staggered delays when scrolled into view
    8. ThemeToggle in navbar switches light/dark mode
    9. LanguageSwitcher in navbar switches all text EN/ES
    10. Mobile: navbar shows hamburger menu, hero stacks vertically, features stack to 1 column
  </verify>
  <done>
    Navbar with sticky blur, mobile menu, language switcher, and theme toggle is complete. Hero section with dramatic gradient, animated tagline, CTA buttons, and floating product mockup placeholder is complete. Features section with 6 animated cards in responsive grid is complete. All text goes through Paraglide i18n. Dark mode works across all three sections. Scroll reveal animations fire on features. The visual quality matches the Gumloop/Relume.io creative standard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build How-It-Works, Pricing, CTA, and Footer sections</name>
  <files>
    src/lib/components/landing/how-it-works.svelte
    src/lib/components/landing/pricing.svelte
    src/lib/components/landing/cta.svelte
    src/lib/components/landing/footer.svelte
    src/routes/+layout.svelte
    src/routes/+page.svelte
    messages/en.json
    messages/es.json
  </files>
  <action>
    Build the remaining four sections and compose the complete landing page. Continue using the same design system components, Paraglide i18n, svelte-inview animations, and Tailwind design tokens.

    **IMPORTANT: Use the frontend-design skill for all UI work.** Maintain the vibrant, creative Gumloop/Relume.io aesthetic throughout.

    **How It Works** (`src/lib/components/landing/how-it-works.svelte`):
    - Use Section component with `variant="dark"` and `id="how-it-works"` (dark section for visual rhythm -- alternating with light Features)
    - Section header: Title (`m.landing_how_title()`) + subtitle (`m.landing_how_subtitle()`) centered, white text
    - 3 steps in a horizontal layout on desktop (`grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12`), stacked on mobile
    - Each step:
      - Large numbered circle: `w-16 h-16 rounded-full bg-gradient-to-br from-primary-500 to-accent-500 flex items-center justify-center text-white font-display font-bold text-2xl`
      - Lucide icon below/beside the number (ClipboardEdit for Brief, Sparkles for AI Generate, Rocket for Publish)
      - Step title: `font-display font-semibold text-xl text-white mt-4`
      - Step description: `text-neutral-400 text-sm mt-2 leading-relaxed`
    - Connecting lines between steps on desktop: Use CSS `::after` pseudo-elements or a decorative horizontal line/gradient between the step circles. On mobile, use a vertical dashed line between steps.
    - Scroll-triggered reveal: svelte-inview on the section, staggered 200ms delay per step
    - The 3 steps (per research discretion recommendations):
      1. "Brief your product" -- describe your product, audience, and brand voice in minutes
      2. "AI generates your strategy" -- get a full content plan with original copy and visuals
      3. "Review and publish" -- approve, edit, or regenerate, then auto-publish to LinkedIn
    - All text via Paraglide messages

    **Pricing** (`src/lib/components/landing/pricing.svelte`):
    - Use Section component with `variant="light"` and `id="pricing"`
    - Section header: Title (`m.landing_pricing_title()`) + subtitle (`m.landing_pricing_subtitle()`) centered
    - 3 pricing tiers in `grid grid-cols-1 md:grid-cols-3 gap-8` with the Pro tier slightly scaled up
    - Each tier uses the Card component:
      - **Beta badge** on ALL tiers: absolute positioned `-top-3 left-1/2 -translate-x-1/2`, using Badge with accent variant, text `m.landing_pricing_beta_badge()` ("Free during beta")
      - Tier name: `font-display font-bold text-xl`
      - Price: `text-4xl font-bold mt-4` with the actual price crossed out next to it: `text-sm opacity-70 line-through ml-2` showing `m.landing_pricing_coming_soon()` or the future price
      - All prices show $0 as current price with the real price struck through
      - Description: `text-sm opacity-80 mt-2`
      - Feature list: `<ul>` with Check icons (Lucide), `space-y-3 mt-6`
      - CTA button at bottom: Button component (primary for highlighted tier, outline for others)
    - Tier details (from PROJECT.md):
      - Free: $0/mo, 1 product, 1 social account, 10 posts/month
      - Pro: $19/mo, 3 products, 5 social accounts, 100 posts/month, Autopilot mode, No watermark (HIGHLIGHTED -- use Card `variant="highlighted"` and `scale-105`)
      - Agency: $49/mo, 10 products, 20 social accounts, Unlimited posts, Team members (3), Priority support
    - All tier feature strings must go through Paraglide i18n
    - Scroll-triggered reveal: staggered 150ms per card
    - Hover: cards lift slightly, highlighted card has glow

    **CTA** (`src/lib/components/landing/cta.svelte`):
    - Use Section component with `variant="gradient"` (matching hero gradient for visual bookending)
    - Centered content, max-width container
    - Title: `m.landing_cta_title()` -- bold, large, white, `font-display font-bold text-3xl md:text-4xl lg:text-5xl`
    - Description: `m.landing_cta_description()` -- `text-lg text-neutral-300 mt-6 max-w-2xl mx-auto`
    - Large CTA Button: primary, lg size, `m.landing_cta_button()`, href="/signup"
    - Subtle decorative elements: gradient blobs similar to hero, low opacity
    - Scroll reveal: fade-in-up animation

    **Footer** (`src/lib/components/landing/footer.svelte`):
    - Background: `bg-neutral-950 text-neutral-400` (always dark, regardless of theme)
    - Layout: `grid grid-cols-1 md:grid-cols-4 gap-8` inside Container
    - Column 1: Logo "BriefAgent" + tagline `m.footer_tagline()` in small text
    - Column 2: Product links (Features, Pricing, How it works)
    - Column 3: Company links (About, Blog, Contact -- placeholder hrefs for now)
    - Column 4: Legal links (Privacy, Terms -- placeholder hrefs)
    - Bottom bar: Horizontal separator + copyright text `m.footer_copyright()` with current year + "Built with AI" or similar
    - All text via Paraglide messages
    - No scroll animations on footer (it's always at the bottom)

    **Complete the page** (`src/routes/+page.svelte`):
    - Import all 5 section components: Hero, Features, HowItWorks, Pricing, CTA
    - Render in order: Hero, Features, HowItWorks, Pricing, CTA
    - Add `<svelte:head>` with page title and meta description via Paraglide messages

    **Complete the layout** (`src/routes/+layout.svelte`):
    - Add Footer component after `{@render children()}`
    - Navbar is already added from Task 1

    **Update i18n**: Add any new message keys to both `messages/en.json` and `messages/es.json`:
    - How-it-works step titles and descriptions
    - Pricing tier feature strings (all individual features for each tier)
    - CTA title, description, button text
    - Footer links and copyright
    - Page meta title and description
    - Ensure ALL pricing feature strings are in i18n (e.g., `landing_pricing_free_feature_1`: "1 product")

    **Visual flow and rhythm:**
    - Hero: DARK gradient (dramatic, premium)
    - Features: LIGHT (clean, organized)
    - How It Works: DARK (visual contrast, emphasis)
    - Pricing: LIGHT (clean, easy to compare)
    - CTA: DARK gradient (matches hero, bookends the page)
    - Footer: DARK (standard)

    This alternating dark/light creates the visual rhythm that reference sites like Gumloop and Relume.io use.
  </action>
  <verify>
    1. `npm run build` completes without errors
    2. `npm run dev` shows the COMPLETE landing page: Navbar -> Hero -> Features -> How It Works -> Pricing -> CTA -> Footer
    3. How-it-works shows 3 steps with numbered circles and connecting elements
    4. Pricing shows 3 tiers with beta badges, $0 current price, crossed-out future price, feature lists
    5. Pro tier is visually highlighted (scaled up, glow, different background)
    6. CTA section has gradient background matching hero and large signup button
    7. Footer shows 4-column grid with links and copyright
    8. ALL text switches between EN/ES via language switcher
    9. ALL sections support dark mode via theme toggle
    10. Scroll animations fire on How-It-Works, Pricing, and CTA sections
    11. Page title and meta description are set via Paraglide
    12. Mobile responsive: all sections stack appropriately, nav has hamburger menu
    13. Smooth scrolling works when clicking nav links (Features, How it works, Pricing)
  </verify>
  <done>
    The complete landing page is built and functional: Navbar (sticky blur, mobile menu, language/theme toggle) -> Hero (gradient, tagline, CTA, product mockup) -> Features (6 animated cards) -> How It Works (3 steps with connectors) -> Pricing (3 tiers with beta badges) -> CTA (gradient, signup button) -> Footer (4-column grid). All text is in EN/ES via Paraglide. Dark mode works throughout. Scroll-triggered animations fire on every below-fold section. Hover micro-interactions on buttons (scale/glow) and cards (lift/shadow). The visual design is vibrant and creative, matching the Gumloop/Relume.io aesthetic.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify landing page quality and completeness</name>
  <files>src/routes/+page.svelte</files>
  <action>
    Human verification checkpoint. Claude has built the complete BriefAgent landing page with:
    - Sticky navbar with backdrop blur, navigation links, language switcher (EN/ES), dark mode toggle, and mobile hamburger menu
    - Hero section with dramatic gradient background, animated tagline "The AI marketing agency you can brief in 5 minutes", product description, two CTA buttons, and floating product mockup placeholder
    - Features section with 6 feature cards (AI Strategy, AI Visuals, Optimized Copy, Publishing, Brand Understanding, Review/Approve) animating in on scroll
    - How-it-works section with 3 numbered steps and connecting visual elements
    - Pricing section with Free/Pro/Agency tiers, all marked "Free during beta" with future prices shown
    - Final CTA section with gradient background and signup button
    - Footer with logo, links, and copyright
    - Full i18n (English and Spanish)
    - Light and dark theme support
    - Rich scroll-triggered animations and hover micro-interactions

    User must verify visually in browser.
  </action>
  <verify>
    1. Open `http://localhost:5173` in your browser
    2. **Overall impression:** Does the page feel vibrant and creative (like Gumloop.com or Relume.io), NOT like a generic SaaS template? Bold colors, strong gradients, polished feel?
    3. **Hero:** Is the gradient dramatic? Is the tagline prominent? Do the CTA buttons look inviting? Does the product mockup placeholder look polished?
    4. **Scroll down:** Do feature cards animate in with staggered delays? Do they have hover lift/shadow effects?
    5. **How it works:** Are the 3 steps clear with visual connectors? Does the dark background create good contrast?
    6. **Pricing:** Are all 3 tiers showing "Free during beta"? Is the Pro tier visually highlighted? Are feature lists complete?
    7. **CTA:** Does it bookend the page with a matching gradient to the hero?
    8. **Dark mode:** Click the theme toggle. Do ALL sections look good in dark mode?
    9. **Language:** Click "ES" in the language switcher. Does ALL text switch to natural Spanish?
    10. **Mobile:** Resize to mobile width. Does the hamburger menu work? Do all sections stack properly?
    11. **Nav links:** Click "Features", "How it works", "Pricing" in the navbar. Does it smooth-scroll to each section?
    Resume signal: Type "approved" if the landing page meets quality expectations, or describe specific issues to fix.
  </verify>
  <done>
    User has visually verified the landing page quality, confirming it matches the Gumloop/Relume.io creative standard with vibrant design, working animations, complete i18n, dark mode support, and mobile responsiveness.
  </done>
</task>

</tasks>

<verification>
Phase 1 is complete when ALL of these are true:
1. Complete landing page renders at root route with hero, features, how-it-works, pricing, CTA, footer
2. Sticky navbar with backdrop blur, nav links (smooth scroll to sections), language switcher, theme toggle, mobile menu
3. All text in EN/ES via Paraglide i18n -- no hardcoded strings
4. Dark/light mode works across all sections without FOUC
5. Rich animations: CSS fade-in on hero (no SSR flash), scroll-triggered reveals on features/how-it-works/pricing/CTA, hover micro-interactions on buttons and cards
6. Responsive: mobile-first, all sections stack on mobile, hamburger menu works
7. Design tokens in @theme: OKLCH colors, Plus Jakarta Sans + Inter fonts, shadows, animations, spacing
8. Reusable components: Button, Card, Badge, Section, Container, ThemeToggle, LanguageSwitcher -- ready for Phase 2
9. `npm run build` succeeds
10. User has visually verified the page quality matches the Gumloop/Relume.io creative standard
</verification>

<success_criteria>
- Landing page is live at localhost with all 7 sections (navbar, hero, features, how-it-works, pricing, cta, footer)
- Page communicates BriefAgent's value proposition clearly: tagline, features, pricing
- Design is vibrant and creative -- not generic SaaS blue
- All requirements met: LAND-01 (hero), LAND-02 (features), LAND-03 (how-it-works), LAND-04 (pricing), LAND-05 (CTA), LAND-06 (vibrant design)
- i18n complete (EN/ES), dark mode complete, mobile responsive, animations polished
- User has approved the visual quality via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page-design-system/01-02-SUMMARY.md`
</output>
